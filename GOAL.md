# GOAL — finish the June conversion

Single entrypoint. Read this first, work top-down, stop when a gate fails.
Build history and design rationale live in `INFERNO-BUILD.md`; this file is the
backlog and the definition of done.

Baseline taken 2026-08-13, immediately after the dashboard chart conversion:

```
conversion: 118/326 files (36.2%), 14674 legacy utilities across 113384 lines
224 test files / 1588 tests green · june-lint clean across 129 files
```

---

## The one number

```bash
cd inferno-frontend && node scripts/conversion-status.mjs
```

`filesRemaining` and `legacyUtilitiesRemaining` must **only ever go down**.
`--json` for machine use. This reports; it does not gate. The gate is below.

---

## GATES — every one must pass before any commit

Run from `inferno-frontend/`. A red gate means stop and fix, never commit past it.

| # | command | pass condition |
|---|---------|----------------|
| 1 | `npm run typecheck` | zero output after the banner |
| 2 | `node scripts/june-lint.mjs` | `clean across N files` |
| 3 | `npm run test:run` | all green, **and the test count never drops** |
| 4 | `npm run build` | `built in …`, no new errors |
| 5 | `./scripts/check-divergence.sh` | every changed file appears in the divergence ledger below |
| 6 | `node scripts/conversion-status.mjs` | remaining count strictly lower than the line above |

Gate 5 changed meaning on 2026-08-15. It used to require the diff be **empty**.
That was right while Inferno was a pure restyle and wrong the moment it started
becoming its own product: upstream is not going to grow the features we want,
so backend divergence is the goal, not a defect. Requiring zero forbade the
product from existing.

It now requires that divergence be **declared**. The failure it still catches is
the one that actually happened: `84a3c4ac`, a commit whose subject said
`feat(ui):`, quietly regenerated ent and added a migration. Accidental drift is
still fatal. Deliberate drift is legal, listed, and survives reconciles.

**Measure against the base HEAD sits on, never against a freshly fetched
`upstream/main`.** Against a stale ref it under-reports; against a fresh one it
counts upstream's own movement as our drift. On 2026-08-15 the same tree read
19 files against the base and 246 against fresh upstream, of which 227 were
upstream moving on. `check-divergence.sh` uses `git merge-base`, which is the
only ref that answers "what have *we* changed". The daily reconcile gets this
right by rebasing (step 3) *before* asserting (step 4).

### The divergence ledger

Anything under `backend/`, `frontend/`, `deploy/` or `docs/` must appear here
**and** in `DECLARED` inside `scripts/check-divergence.sh`. A file that differs
and is not listed is a bug, not a feature.

| # | area | files | why | re-apply after rebase |
|---|------|-------|-----|-----------------------|
| D1 | `avatar_seed` on `users` | `ent/schema/user.go` (3 lines) + `ent/` regeneration (8 files, 286 lines) + `dto/{types,mappers}.go`, `user_handler.go`, `api_key_repo.go`, `user_repo.go`, `service/user.go`, `user_service.go` (15 lines) + `migrations/900_add_user_avatar_seed.sql` | Persists a regenerated identicon server-side so it survives reload and syncs across devices. Upstream stores avatars in a separate table and has no seed concept. | **Do not hand-merge the `ent/` files.** Re-run `go generate ./ent` and let codegen rebuild from the 3-line schema. Only `ent/mutation.go` realistically conflicts — it is shared across every entity, so any upstream schema change touches it. |
| D2 | English legal-document defaults | `service/setting_public.go` (4 strings) + `server/api_contract_test.go` (golden fixture) | Chinese defaults on the legal documents of a rebranded English product. Applies only when no `login_agreement_documents` settings row exists — i.e. a fresh install, which is exactly when it matters. | Trivial. Zero upstream commits to this file in the 124 we were behind. |
| D3 | `.gitignore` | appended negations | Lets `inferno-frontend/` and `docs/superpowers/` be tracked (upstream allowlists `docs/*`). Outside the four gated dirs, so the script does not see it; listed for completeness. | Trivial. |
| D4 | OpenComputer portal design specs | `docs/superpowers/specs/*.md` | Design docs for making Inferno the OC Portal (OAuth authorization server, agent registry, billing contract) — replacing Nous Portal for the hermes-agent client. Docs only, no code. Upstream has no equivalent and never will. | Trivial — additive files, cannot conflict. |
| D5 | **Razorpay payment provider** | 6 new files (`payment/provider/razorpay{,_test}.go`, `payment/razorpay_events.go`, `service/payment_client_verification.go`, `service/payment_razorpay_subscription{,_test}.go`) + 12 modified (`payment/provider/factory.go`, `payment/types.go`, `handler/payment_{handler,webhook_handler}.go`, `server/routes/payment.go`, `service/payment_{service,order,currency,fulfillment,config_providers,order_provider_snapshot,webhook_provider}.go`) | **Core product, permanent.** Inferno bills Indian customers; upstream ships Stripe/epay/Alipay and no INR path. This is not a patch waiting to be retired — it is one of the reasons the fork exists. | Additive files cannot conflict. The 12 modified ones are the seams where a provider plugs in — `factory.go` (registration switch), `types.go` (provider enum), and the `payment_*` service files (per-provider branches). Upstream touched `backend/internal/{payment,service,handler}` in 6 commits since the last base, so expect real conflicts here eventually. Resolve by **keeping upstream's structure and re-adding the Razorpay branch**, never by taking our whole file — that is how you silently drop an upstream fix. |
| D6 | `NODE_OPTIONS` heap bump | `deploy/Dockerfile` (1 line, 1536 → 3072) | The frontend build OOMs at upstream's 1536 MB because Inferno's bundle is larger. Unrelated to payments — it was committed alongside `318a7852` and only looks like payment work. | Trivial, but **check whether it is still needed** rather than re-applying blind: if a future upstream bumps it themselves, drop this row. Upstream's own Dockerfile edits so far are on other lines (they bumped `GOLANG_IMAGE` to 1.26.6 in `11e1e228`), so git merges both cleanly. |
| D7 | OAuth authorization server (org tenancy + RS256 signing key/JWKS + oauth_client registry + device authorization request) | `ent/schema/{org,org_member,oauth_client,oauth_device_authorization}.go` + `ent/` regeneration (44 files, incl. incidental `ent/group.go` comment-only drift picked up by the same `go generate ./ent` run, and Task 4's 7 new `ent/oauthdeviceauthorization*` files plus 9 further-modified shared codegen files) + `migrations/901_org_and_members.sql` + `migrations/904_org_personal_user_id.sql` + `migrations/902_oauth_client.sql` + `migrations/903_oauth_device_authorization.sql` + `service/{org_service,org_service_test}.go` + `service/{auth_service,wire}.go` + `cmd/server/wire_gen.go` + `cmd/jwtgen/main.go` + 16 test files threading the new `NewAuthService` `orgService` param + `service/{oauth_signing_key,oauth_signing_key_test}.go` + `service/{oauth_client_service,oauth_client_service_test}.go` + `service/{oauth_device_service,oauth_device_service_test}.go` + `handler/oauth_handler.go` + `handler/{handler,wire}.go` + `handler/dto/oauth.go` + `server/routes/{oauth,common}.go` + `server/router.go` + `service/{oauth_token_service,oauth_token_service_test}.go` + `service/refresh_token_cache.go` + `repository/refresh_token_cache.go` + `repository/refresh_token_cache_test.go` + `handler/oauth_handler_test.go` + `server/routes/oauth_token_route_test.go` + `server/middleware/{oauth_scope,oauth_scope_test}.go` + `server/routes/oauth_account_route_test.go` + `handler/oauth_device_decision_test.go` + `inferno-frontend/src/{api/oauth.ts,router/index.ts,views/oauth/DeviceApprovalView.vue,views/oauth/__tests__/DeviceApprovalView.spec.ts,i18n/locales/en/misc.ts,i18n/locales/zh/misc.ts}` + `migrations/905_hermes_cli_first_party_client.sql` + `scripts/oauth-conformance.md` + final whole-branch fix wave: `service/oauth_scope_vocabulary.go` + `service/auth_service_personal_org_login_test.go` + `inferno-frontend/src/components/layout/InterstitialState.vue` + `migrations/907_oauth_rs256_key.sql` + authorization_code + PKCE plan Task 2: `service/{oauth_redirect_uri,oauth_redirect_uri_test}.go` + Task 3: `ent/schema/oauth_authorization_code.go` + `ent/` regeneration (new `oauthauthorizationcode{,_create,_delete,_query,_update}.go` + `oauthauthorizationcode/{oauthauthorizationcode,where}.go`; the shared codegen files this run also touched -- `client.go`, `ent.go`, `hook/hook.go`, `intercept/intercept.go`, `migrate/schema.go`, `mutation.go`, `predicate/predicate.go`, `runtime/runtime.go`, `tx.go` -- are the same files Task 3's (oauth_client registry) earlier `go generate ./ent` run already touched, already listed above) + `migrations/906_oauth_authorization_code.sql` + `service/{oauth_authorize_service,oauth_authorize_service_test}.go` (`oauth_token_service.go`, `oauth_handler.go`, `oauth_scope_vocabulary.go` are already listed above) + authorization_code + PKCE plan Task 4 (`GET`/`POST /oauth/authorize` and the consent screen): `handler/{oauth_authorize_handler,oauth_authorize_handler_test}.go` + `internal/web/embed_on.go` (bypasses `/oauth/authorize` from the embedded-frontend SPA fallback so this real backend endpoint runs instead of index.html) + `inferno-frontend/src/{api/oauth.ts,router/index.ts,views/oauth/AuthorizeConsentView.vue,views/oauth/__tests__/AuthorizeConsentView.spec.ts,i18n/locales/en/misc.ts,i18n/locales/zh/misc.ts}` (`oauth_handler.go`, `oauth_authorize_service.go` -- `ValidCodeChallengeShape` exported, no behavior change --, `handler/wire.go`, `service/wire.go`, `server/routes/oauth.go`, `server/router.go`, `cmd/server/wire_gen.go` are already listed above) + Task 4 fix round 1 (F1-F15 from review, `.superpowers/sdd/2026-08-18-authorization-code-pkce/task-4-review.md`): `internal/web/embed_test.go` (was already touched by the original Task 4 commit -- a new `TestEmbeddedFrontendBypassesOAuthAuthorize` -- but never declared; review's F3 caught the resulting divergence-gate failure) + `internal/web/bypass.go` + `internal/web/bypass_test.go` (F4: `shouldBypassEmbeddedFrontend` and its tests moved out of the two `//go:build embed` files into these two untagged ones, adding `/.well-known/jwks.json` to the allowlist alongside `/oauth/authorize` in the same change, so both the pre-existing JWKS gap and the route this task added are covered by an assertion that actually runs under a plain `go test ./...`) + Task 4 fix round 2 (NEW-1 to NEW-11 from the re-review, `.superpowers/sdd/2026-08-18-authorization-code-pkce/task-4-rereview.md`): `internal/server/middleware/{panel_rate_limit,panel_rate_limit_test}.go` (NEW-4: added `PublicIPScoped`, a variant of the existing `PublicIP` keyed by an extra scope segment, so `/oauth/authorize`'s rate-limit bucket no longer shares one counter with Model Plaza's public group for the same client IP) + `internal/web/embed_off.go` (NEW-8: removed the package doc comment that duplicated the one `bypass.go` picked up in fix round 1) + `inferno-frontend/src/router/loginRedirect.ts` (new file -- NEW-1: extracted the F1 redirect-resolution branch out of `router/index.ts` into its own importable function, mirroring the existing `setupRedirect.ts` pattern, so `router/index.ts`'s guard and `router/__tests__/guards.spec.ts` consume the SAME definition instead of the test asserting a hand-copied mirror; verified by reverting the function to the pre-fix behaviour, confirming the two F1 test cases fail, and restoring it) (`oauth_authorize_handler.go`, `oauth_authorize_handler_test.go`, `oauth_authorize_service.go`, `server/router.go`, `server/routes/oauth.go`, `handler/oauth_handler.go`, `inferno-frontend/src/router/index.ts`, `inferno-frontend/src/router/__tests__/guards.spec.ts` are already listed above -- not re-listed here, same files, doc-comment and stale-pointer fixes only for NEW-3/NEW-5/NEW-6/NEW-7/NEW-10) + oauth-bearer-inference plan Task 2 (`api_keys.oauth_client_id` -- the backing-row column and its partial unique index): `ent/schema/api_key.go` + `ent/` regeneration (`apikey.go`, `apikey/{apikey,where}.go`, `apikey_{create,update}.go`, `migrate/schema.go`, `mutation.go`, `runtime/runtime.go` -- `apikey_query.go`/`apikey_delete.go` untouched) + `migrations/909_api_key_oauth_client_id.sql` + `internal/repository/api_key_oauth_client_id_integration_test.go` + oauth-bearer-inference plan Task 3 (`OAuthBackingKeyService` -- get-or-create of the api_keys row a verified OAuth access token resolves to): `internal/service/{oauth_backing_key,oauth_backing_key_test}.go` + `internal/config/config.go` (the `oauth_backing_key.group_name` policy + its viper default) + `internal/service/api_key_service.go` (`GenerateAPIKeySecret` extracted from `APIKeyService.GenerateKey`) + `internal/service/wire.go` (provider registration; `cmd/server/wire_gen.go` is byte-identical after regeneration because nothing depends on the service until Task 4's middleware does) + Task 3 fix round 1 (review findings F1-F9): `migrations/910_api_key_oauth_client_uniq_live_only.sql` + `internal/service/{api_key,api_key_service,api_key_service_delete_test}.go` + `internal/repository/api_key_repo.go` + `deploy/config.example.yaml` + oauth-bearer-inference plan Task 4 (the `/v1` OAuth credential branch -- the middleware that finally makes an Inferno-issued access token work on Inferno's own inference endpoints): `internal/server/middleware/{oauth_inference_auth,oauth_inference_auth_test}.go` + `internal/server/middleware/ingress_reject.go` (three OAuth-specific admission-failure reasons) + `internal/server/routes/{gateway,gateway_test,gateway_key_billing_test,gateway_oauth_mount_test}.go` + `internal/server/{router,http}.go` + `cmd/server/wire_gen.go` + `internal/repository/api_key_repo.go` (`APIKeyEntityToService` exported) + `internal/handler/oauth_handler.go` (nil-receiver-safe accessors) | OAuth authorization server plan Task 1: minimal org tenancy so every later task can reference `org_id`. Every user gets an idempotent personal org on signup (email or OAuth), created best-effort — failure logs a warning and does not block signup, since `EnsurePersonalOrg` repairs on next login. Migration numbering: 900 already taken (D1), so this is 901, first of the fork's 9xx series; 902/903 are reserved by this same plan's later tasks (oauth_client, oauth_device_authorization), so the concurrency-safety follow-up below is 904. Ledger numbering: D4 already taken (design specs, committed same day), so this is D5. Org-role constants are named `OrgRoleOwner`/`OrgRoleAdmin`/`OrgRoleMember` (not `RoleOwner`/`RoleAdmin`/`RoleMember`) because `service.RoleAdmin` already exists as the platform-wide user role (`domain.RoleAdmin == "admin"`); the wire-format string values themselves (`"OWNER"`/`"ADMIN"`/`"MEMBER"`, read verbatim by the desktop client) are unchanged. `migrations/904_org_personal_user_id.sql` adds `orgs.personal_user_id` (nullable, unique where non-null) — a Task 1 review found `EnsurePersonalOrg`'s original read-then-create had no DB-level invariant, so two near-simultaneous calls for a new user (double-fired OAuth callback, two tabs) could each create a distinct personal org; `EnsurePersonalOrg` now looks up and inserts by `personal_user_id` and treats a unique-constraint violation on create as "lost the race, re-read the winner's row" rather than an error. Upstream has no tenancy concept. **Task 2:** ES256 signing keypair + public JWKS endpoint at `/.well-known/jwks.json` (server root, not `/api`), so agents/services can verify OAuth-issued tokens offline. The keypair is generated on first `Active()` call and persisted PEM-encoded in the existing `security_secrets` table under key `oauth_es256_active` — no schema change, no new migration. `Active()` handles the same concurrent-first-call race as `EnsurePersonalOrg`: a lost race on the `security_secrets.key` unique constraint (detected via `dbent.IsConstraintError`, not string matching, since prod is Postgres and tests are SQLite) re-reads and returns the winner's key rather than erroring or minting a second, orphaned key. JWKS projection never includes the private scalar `d`; `x`/`y` are left-padded to a fixed 32 bytes via `b64uint` since P-256 coordinates are fixed-width. OAuth handlers deliberately bypass `internal/pkg/response`'s `{code,message,data}` wrapper — external clients parse RFC-shaped JSON at the top level. **Task 3:** `oauth_client` registry (`oauth_clients` table) + `POST /api/oauth/self-hosted-client`, bearer-authenticated, mounted at the server root under `/api/oauth` (a new `RegisterOAuthAPIRoutes`, sibling to `RegisterOAuthWellKnown`) rather than under the versioned `/api/v1` panel API, because this surface is consumed by the hermes CLI / dashboard and shares its path prefix with later unauthenticated OAuth endpoints (device/code, token) in this same plan. `oauth_clients` are PUBLIC OAuth clients — no `client_secret` column; PKCE is the protection. `client_id` is always server-generated as `agent:{32 hex chars}`; callers never choose it, since a client that could pick its own id could impersonate another agent. `name` (docker-style `adjective_noun`) is deliberately NOT unique — the row id is the key, so collisions are harmless and never block registration. `instance_id` is nullable and unique (partial index, `WHERE instance_id IS NOT NULL`, mirroring `orgs_personal_user_id_key`) but nothing sets it yet — the column exists now so a later oc-platform sub-project can make VM provisioning idempotent on it without a further migration. The handler resolves the caller's org via `OrgService.OrgsForUser` and uses `orgs[0]` (every user has exactly one personal org per Task 1); an empty slice returns a clean 500 rather than indexing out of bounds. Logging is restricted to `client_id` (public by design) and `org_id` — never `user_id`, tokens, or secrets. Verified live: built the binary, ran it against a throwaway Postgres+Redis, logged in as the auto-setup admin, seeded that user a personal org directly (auto-setup's admin bypasses the normal signup path that calls `EnsurePersonalOrg`, so it has none by default), and confirmed `POST /api/oauth/self-hosted-client` with a bearer token returns a bare `{"client_id":"agent:...","name":"..."}` (not `{code,message,data}`-wrapped) while the same call with no `Authorization` header returns 401; two registrations with colliding-possible names both succeeded; server logs contained `client_id`/`org_id` only, never the bearer token or admin password. **Task 4 (this addition):** RFC 8628 device authorization request, `POST /api/oauth/device/code` — a new `oauth_device_authorizations` table plus `service.OAuthDeviceService.RequestCode`. Deliberately UNAUTHENTICATED (a new `RegisterOAuthDeviceRoutes`, a second `/api/oauth` group registered alongside the bearer-gated `RegisterOAuthAPIRoutes`, not merged into it): a headless CLI calling this endpoint has no session yet, `client_id` is the request's only identity, and it is validated against the Task 3 `oauth_client` registry (`OAuthDeviceService` composes an internal `*OAuthClientService` and calls its `ByClientID`, rather than re-querying the ent client directly, so client validation stays in one place). `user_code` is an 8-character, human-typed code drawn from `service.UserCodeAlphabet` (`ABCDEFGHJKMNPQRSTUVWXYZ23456789` — no `0/O`, no `1/I/L`); `device_code` is 32 bytes of `crypto/rand` hex-encoded, and its `Read` error is checked (an unchecked failure would silently degrade to a predictable code, letting an attacker redeem someone else's pending login). Both columns are schema-`Unique()`; a `device_code` collision cannot practically happen at 256 bits of entropy, but a `user_code` collision is handled by regenerating and retrying the insert (bounded at 5 attempts, detected via `dbent.IsConstraintError`) rather than surfacing a raw constraint-violation 500 to an innocent caller. `verification_uri` is built from `cfg.Server.FrontendURL` (`{frontend_url}/device`) and `verification_uri_complete` embeds the URL-escaped `user_code`; `FrontendURL` defaults to `""` for deployments that never set it, and rather than silently emitting an unusable relative URL, `RequestCode` refuses with `service.ErrPortalNotConfigured` (mapped to a 500 `server_error`, logged at Error level with no request-derived fields) when it is empty — a fail-loud choice over failing the whole server at startup, since not every deployment necessarily uses the device flow. `expires_in` is 900s (15 min), `interval` is 5s. Neither `device_code` (a bearer credential until redeemed) nor `user_code` (also unwise) is ever logged; handler error paths log `client_id` only, and the "unknown client_id" and "portal not configured" paths are logged at different levels (`Warn` vs `Error`) but return generically-worded bodies so a probing caller can't distinguish response shapes. `Approve`/`Deny` (consumed by Task 5, the approval endpoint) look up by `user_code`, normalize it (`TrimSpace`+`ToUpper`, so a human typing lowercase or with stray whitespace still matches), and reject with `ErrDeviceCodeNotFound`/`ErrDeviceCodeExpired` as unwrapped sentinels so handler code can `errors.Is` them. All OAuth handlers in this row continue to bypass `internal/pkg/response`'s wrapper — verified live (see report). **Task 5 (this addition):** `POST /api/oauth/token`, the device_code and refresh_token grants — the task that actually mints credentials, on the same unauthenticated `/api/oauth` group as `device/code` (the caller has no session, that is the point of a token endpoint). `service.OAuthTokenService.mintAccessToken` signs ES256 JWTs itself (`jwt.NewWithClaims(jwt.SigningMethodES256, claims)`, `Header["kid"] = key.Kid`, keyed via Task 2's `OAuthKeyService.Active`) and deliberately does NOT call `AuthService.GenerateTokenPair` — that mints Inferno's separate HMAC-signed panel-session token, which Task 6's resource-server middleware (ES256-only, by design) would silently reject two tasks later. Refresh tokens ARE persisted through the existing Redis-backed `RefreshTokenCache` (`internal/service/refresh_token_cache.go`) rather than a parallel store: `RefreshTokenData` gained three `omitempty` fields — `Scope`, `ClientID` (absent from the panel-session shape, needed here so a refresh can validate both), and `Rotated` (a tombstone marker, added beyond the two the plan anticipated). Rotation does NOT hard-delete the presented token on success; it re-stores the same hash with `Rotated=true` for the remainder of its original TTL. That tombstone is what makes reuse detection possible at all: a replay of that exact raw token is looked up successfully, sees `Rotated=true`, and calls `DeleteTokenFamily(data.FamilyID)` — which deletes every hash the Redis-backed cache ever added to that family set, including the token the legitimate client received from the rotation. A hard-delete-on-rotation design (which is what `AuthService.RefreshTokenPair`, the panel-session equivalent, actually does — `ErrRefreshTokenReused` is declared there but never returned; nothing in that path currently re-derives the family from a deleted hash) would have made this indistinguishable from "never existed" and downgraded reuse detection to a silent no-op; `oauth_token_service_test.go`'s `TestRefreshTokenReuseIsRejected` asserts both that the replay itself fails AND that the token from the legitimate rotation is also dead afterward, specifically to catch a single-token-delete regression. Device-code redemption is single-use via a status-guarded `UPDATE ... WHERE status='approved'` (`OAuthDeviceAuthorization.Update().Where(ID(row.ID), Status("approved")).SetStatus("expired"))`, not the plan's unconditional update, so two concurrent redemptions of one human approval can't both mint a token pair. The RFC 8628 §3.5 error strings (`authorization_pending`, `slow_down`, `access_denied`, `expired_token`) are unwrapped sentinel errors the hermes client branches on verbatim; `slow_down` fires by comparing `time.Since(*row.LastPolledAt)` against the poll `interval` and — critically — only advances `LastPolledAt` on a poll that is NOT itself rejected as `slow_down`, so a client can't reset the backoff by polling continuously. `refresh_token` grant failures (unknown token, wrong `client_id`, expired, and reuse) all collapse to RFC 6749 §5.2's single `invalid_grant` on the wire — distinguishing them would let a caller enumerate token/client existence — with the reuse case additionally logged server-side (`client_id` only, per the no-credential-logging rule that holds throughout this row). `issuer` is wired from `cfg.Server.FrontendURL`, the same public origin Task 2's JWKS is served from, via a new `ProvideOAuthTokenService` (`service/wire.go`) threaded through `cmd/server/wire_gen.go` by hand (no `wire` binary in this environment, consistent with how Tasks 3-4 updated it). No ent schema change was needed — Task 4 had already added `oauth_device_authorizations.last_polled_at` in anticipation of this task. Verified live: built the binary against a throwaway Postgres+Redis, registered a client, requested a device code, polled once (`authorization_pending`), polled again immediately (`slow_down`), approved the row directly in Postgres, polled again (200 with `access_token`/`refresh_token`/`scope`), decoded the access token header (`alg: ES256`, non-empty `kid`), redeemed the same `device_code` a second time (`expired_token`), rotated the refresh token once, then replayed the original refresh token and confirmed both the replay and the previously-rotated token now fail — see report for the full transcript. **Task 5 review fixes (this addition):** an independent review of Task 5 (run on a stronger model, since this is where credentials are actually minted) found `ExchangeRefreshToken`'s "tombstone" design sound but its Go-side implementation racy: reading `RefreshTokenData` then separately re-storing it as rotated was two Redis round trips, so two concurrent presentations of the SAME refresh token (an attacker replaying a stolen token at the same instant the legitimate client refreshes on schedule) could both observe `Rotated=false` and both mint a token pair — forking the family into two live branches that never again trip the reuse detector, since neither side ever re-presents an already-rotated token to the other. Fixed by adding `RefreshTokenCache.MarkRotated`, a single atomic GET-decide-SET Redis operation (`internal/repository/refresh_token_cache.go`'s `refreshTokenMarkRotatedScript`, a Lua `EVAL` using `cjson.decode` to check the `rotated` field and Redis 6's `SET ... KEEPTTL` to flip it while preserving the record's exact remaining TTL — no separate PTTL-read-then-SET-PX window to race on either); `ExchangeRefreshToken` now calls it *before* minting anything, so a mid-flight crash costs at most the one token being rotated, never a forked family. `TestExchangeRefreshTokenConcurrentPresentationsOnlyOneWins` (service layer, in-memory fake mirroring the same atomicity under its own mutex) and `TestRefreshTokenCache_MarkRotatedConcurrentOnlyOneWins` (repository layer, real Lua `EVAL` against miniredis) both fire N=20/50 goroutines at one token under `-race` and assert exactly one wins; mutation-tested by temporarily swapping `ExchangeRefreshToken`'s body back to the pre-fix read-then-write shape and confirming the SAME test then fails (typically all N presentations "succeed" — see task-5-report.md for the exact before/after runs). The review's second finding, `ExchangeRefreshToken` never re-validating the account, is also closed: `OAuthTokenService` now takes a minimal `OAuthUserLookup` (just `GetByID` — deliberately narrower than the full `UserRepository` so any real implementation satisfies it structurally, per Go's implicit interface satisfaction, with no adapter) and revokes the whole token family via `DeleteTokenFamily` if the user is missing or inactive, mirroring `AuthService.RefreshTokenPair`'s equivalent check — closing the gap where `UserService.UpdateStatus`/`Delete` invalidate an auth cache but never call `RevokeAllUserSessions`, so a banned user's OAuth refresh token would otherwise keep minting fresh access tokens for the rest of its 30-day life. Third: the `Token` handler previously folded every unmatched internal error (a Redis outage, a signing-key read failure) into an OAuth error code (`expired_token`/`invalid_grant`) with no server-side log line — indistinguishable from a real expired/invalid credential and silent to an operator; it now matches known sentinels explicitly (including `service.ErrDeviceCodeNotFound`/`ErrDeviceCodeExpired`, missed in the original mapping) and falls through to a logged `500 server_error`, mirroring the pattern `DeviceCode` already used. Fourth: `tokenResponse` now sets `Cache-Control: no-store` and `Pragma: no-cache` per RFC 6749 §5.1, since the body carries a bearer credential an intermediary must never cache. Fifth: `issueRefreshToken`'s `AddToUserTokenSet` failure is now fatal (was logged-and-ignored) — a token silently missing from the user set would survive a password-change/logout-all-devices revocation for the rest of its life. Sixth: `oauth_token_service_test.go`'s reuse test now asserts the exact sentinel (`errors.Is(err, ErrInvalidGrant)`) for the legitimately-rotated token's post-revocation state rather than "any error", so an unrelated failure can't masquerade as successful family revocation. New wire-contract test coverage closes the gap the review flagged (handler-level behavior was previously verified only by a curl transcript, nothing in `go test ./...`): `internal/handler/oauth_handler_test.go` table-tests every `Token` grant/error branch against a real `httptest` request, asserting the exact `{"error":"..."}` body with no `code`/`message`/`data` keys and the no-store headers on success; `internal/server/routes/oauth_token_route_test.go` asserts `POST /api/oauth/token` resolves with no `Authorization` header when only `RegisterOAuthDeviceRoutes` is registered, and (inversely) that `jwtAuth` never runs for it even when `RegisterOAuthAPIRoutes` is also registered on the same engine. Deliberately NOT fixed, per the review's explicit scoping: a narrower residual race between the winner's own `AddToFamilyTokenSet` call and a concurrent loser's `DeleteTokenFamily` call (only reachable when a token's legitimate rotation and its replay are presented at truly the same instant, not the realistic sequential-replay case this feature defends against — noted in task-5-report.md, not exercised by the final concurrency test's assertions); unbounded token-family-set growth; the device-code-consumed-before-minting ordering; and `AuthService.RefreshTokenPair`'s own dead `ErrRefreshTokenReused` (confirmed real, pre-existing, out of scope — the panel-session path is untouched by this row). **Task 6 (this addition):** the consumption side of Task 5's credentials — a new resource-server middleware, `middleware.RequireOAuthScope(keySvc *service.OAuthKeyService, required string) gin.HandlerFunc`, plus `GET /api/oauth/account`. The middleware parses with `jwt.NewParser(jwt.WithValidMethods([]string{"ES256"}))` and additionally asserts `token.Method.(*jwt.SigningMethodECDSA)` inside its keyfunc — two independent checks against algorithm confusion, mirroring `AuthService.ValidateToken`'s existing pattern for the HMAC panel-session side (`auth_service.go:1322`) — specifically so a token signed with Inferno's HMAC panel-session secret (held by every server process) can never be accepted here: that is the entire reason Task 2 introduced a separate ES256 keypair, and accepting both algorithms on this middleware would have quietly defeated it. `scopeSatisfies` is exact, whitespace-split equality (`strings.Fields` + `==`), not `strings.Contains` — a substring match would let a token carrying `billing:manage_nothing` or bare `billing` satisfy a `billing:manage` requirement, a privilege-escalation bug; `billing:manage` is deliberately never granted at initial device-flow login (the desktop re-runs the flow to elevate) and this middleware does not special-case it. `exp` enforcement is `golang-jwt/v5`'s default claims validation (no extra code needed) since `mintAccessToken` always sets it. The verified `sub` claim (a decimal string, per `strconv.FormatInt` in `mintAccessToken`) is parsed with `strconv.ParseInt` and the request rejected outright if it doesn't parse, rather than defaulting `oauth_user_id` to `0` — a zero-valued id reaching a handler would silently act on the wrong account instead of failing closed. Three context keys are set on success — `oauth_user_id` (`int64`), `oauth_client_id` (`string`, from `aud`), `oauth_scope` (`string`) — as exported string constants (`OAuthContextKeyUserID` etc.) in the middleware package rather than the brief's bare string literals, so the handler and the middleware can't drift on the key name independently. `OAuthHandler` gained one accessor, `KeyService() *service.OAuthKeyService`, so `routes.RegisterOAuthAccountRoutes` (a third, new `/api/oauth` route group — distinct from both the jwtAuth-gated `RegisterOAuthAPIRoutes` and the fully-unauthenticated `RegisterOAuthDeviceRoutes`, because an OAuth bearer is a third, different auth shape) can build the middleware without a new wire-provided dependency; no `wire.go`/`wire_gen.go` changes were needed since `OAuthKeyService` and `OAuthHandler` were already wired by Tasks 2 and 3. `GET /api/oauth/account` is registered with `required=""` (any validly-signed, unexpired OAuth token — it grants no elevated capability, it just resolves the caller's own org membership) and returns `{"user_id": "<decimal>", "orgs": [{"id","slug","name","isPersonal","role"}, ...]}`, reusing Task 1's `OrgService.OrgsForUser`/`RoleIn`; a per-org `RoleIn` failure logs and falls back to `service.OrgRoleMember` (this codebase's least-privileged org role) rather than ever inventing an elevated one — the brief's own draft used the nonexistent `service.RoleMember` (that identifier is the *platform* role constant; the org-scoped ones are `OrgRole{Owner,Admin,Member}`, renamed in Task 1 specifically to avoid this collision) and would not have compiled as intended. Test fixtures reuse this package's already-established `newPaymentConfigServiceTestClient`-equivalent pattern (the brief's named helper, `newTestEntClient`, does not exist; `internal/handler/oauth_handler_test.go`'s `newOAuthHandlerTestEntClient` is the same shape one package over and was mirrored, not reused directly, since it's unexported across package boundaries) — real ES256 signature verification against a throwaway sqlite-backed `OAuthKeyService`, not a stub. Beyond the brief's two smoke tests (`nil` keySvc, missing bearer / `alg:none`), `oauth_scope_test.go` adds: valid ES256 token + sufficient scope → 200 with correct context values; valid token + insufficient scope → 403 `insufficient_scope`; no `scope` claim against a non-empty requirement → 403; an HMAC-signed token with otherwise-fully-valid claims (correct `sub`/`aud`/`scope`, future `exp`) → 401 `invalid_token` (the security property that matters most here); an expired ES256 token → 401; an unparsable `sub` → 401. `oauth_handler_test.go` adds the `Account` endpoint's own wire-contract tests reusing `requireBareErrorBody` per this row's standing rule against `internal/pkg/response`: missing bearer, garbage token, and a full round trip (real `EnsurePersonalOrg` row, real minted token, asserts the exact 2-key top-level body and the nested org fields, no `code`/`message`/`data`). Verified live: full `docker-compose`-free flow against a throwaway Postgres+Redis — registered a client, ran the device flow to approval, exchanged for a real access token, then `curl -H "Authorization: Bearer <token>" /api/oauth/account` returned the bare account JSON; the same call with no header and with a garbage token both returned bare `401 {"error":"invalid_token"}`; server logs contained no token material — see task-6-report.md for the full transcript. **Task 7 (this addition):** the device approval screen — the only human-facing step of the whole flow. Go half: `OAuthHandler.ApproveDevice`/`DenyDevice`, `POST /api/oauth/device/approve` and `/device/deny`, registered on the existing jwtAuth-gated `RegisterOAuthAPIRoutes` group (a logged-in human approves in a browser, not a bearer-token agent). These two are a DELIBERATE, commented exception to this row's own bare-JSON rule: every other OAuth handler in this file bypasses `internal/pkg/response` because the hermes client parses RFC-shaped JSON at the top level, but these two are panel-only — never called by hermes — and inferno-frontend's `api/client.ts` response interceptor reads `apiData.code`/`apiData.message` to build the error it shows the user, so a bare `{"error":"..."}` body would surface as a generic, non-actionable axios error on every failure. `ErrDeviceCodeNotFound`/`ErrDeviceCodeExpired` map to `response.NotFound` (404) and `response.Error(c, http.StatusGone, ...)` (410); `user_code`/`device_code` are never logged, same rule as Tasks 4-6. New `internal/handler/oauth_device_decision_test.go` (kept separate from `oauth_handler_test.go`, whose own header comment asserts the opposite, bare-body contract for `/token`) covers missing auth, missing/unknown/expired `user_code`, and a real approve/deny round trip against a seeded `oauth_device_authorizations` row, asserting the envelope shape and the persisted `status`/`approved_user_id`. Vue half, entirely new to this plan (Tasks 1-6 never touched `inferno-frontend/`): `views/oauth/DeviceApprovalView.vue` at route `/device` (exact path required — `OAuthDeviceService.RequestCode` hardcodes `verification_uri` as `{frontend_url}/device`), `requiresAuth: true` so an unauthenticated visitor is bounced to `/login?redirect=/device?user_code=...` and returned with the query intact by the router's existing `redirect` handling (no new code needed there beyond the route entry itself). The `user_code` input is prefilled from `route.query.user_code`, live-uppercased and re-hyphenated at `XXXX-XXXX` on every keystroke (tolerating a pasted lowercase or hyphen-less code) via a computed `v-model` setter; the backend independently trims/uppercases too. Three outcomes past the form: approve/deny (200) are terminal, rendered through the existing `InterstitialState` component (archetype F; this view is its FIRST consumer -- the component's own header used to claim nine OAuth-callback-shaped routes already shared it, which was aspirational, not true, so treat it as new code); an expired code (410) is ALSO terminal, its own screen telling the human to re-run the CLI command, since retyping cannot fix it; an unknown code (404) is deliberately NOT terminal — it stays on the form as an inline `Input` error, since it usually means a mistyped character and the person should be able to fix and resubmit immediately, the same shape as a wrong password on the login screen. New `api/oauth.ts` calls through the shared `apiClient` (so the auth token, `Accept-Language`, and the response-envelope/error interceptor all still apply) but targets `buildGatewayUrl('/api/oauth/device/...')` rather than a bare relative path, since `/api/oauth` lives outside `apiClient`'s `/api/v1` `baseURL` and a relative path would double-prefix. A new top-level `device` i18n namespace was added to `misc.ts` (en + zh) rather than editing `router/index.ts`/`i18n/` files under a swarm-conversion agent's "don't touch" convention — that convention (`inferno-frontend/CONVENTIONS.md`) governs the parallel June-redesign workflow for existing views, not a new backend-plan feature with its own brief explicitly assigning those files. `inferno-frontend/scripts/check-divergence.sh`'s `CHANGED` computation is scoped to `backend frontend deploy docs` from the repo root (`frontend/` there is the pristine upstream mirror, not `inferno-frontend/`), so none of the `inferno-frontend/` files above are in that gate's enforcement scope or its `DECLARED` list — they are listed here in the files column for the record, and in `DECLARED` only the one new backend file (`oauth_device_decision_test.go`) actually falls inside the gate's diff scope. Verified live: threw away Postgres+Redis (`t7-pg`/`t7-redis`, distinct from any other worktree's containers), built the binary, ran `npx vite` against it with `VITE_DEV_PROXY_TARGET` pointed at the throwaway backend, registered a client, requested a device code (`verification_uri_complete` resolved to `http://127.0.0.1:15173/device?user_code=...`), and drove the actual browser through the real UI: unauthenticated visit redirected to `/login?redirect=/device?user_code=...` and landed back on `/device` with the code prefilled after login; clicked Approve and confirmed both the UI's "Device approved" screen and `oauth_device_authorizations.status='approved'`/`approved_user_id` in Postgres; redeemed the same `device_code` at `/api/oauth/token` for a real ES256 access token, closing the loop end to end through the browser rather than a direct-SQL stand-in (Tasks 4-6's necessary workaround before this screen existed). Also verified live: a wrong code stays on the form with an inline error, an expired code shows the terminal "run the command again" screen, and Deny shows its own terminal screen — see task-7-report.md for the full transcript and screenshots. **Task 8 (this addition, final task of this plan):** end-to-end conformance — drove the REAL hermes Python CLI (`hermes_cli/auth.py`, a separate read-only repo, `/Users/saksham/OpenComputerV2/OpenComputerV2` — never edited, never committed to) against this server. Found two plan defects by inspecting the real client, both fixed here rather than in the client (the client is the specification): (1) the real CLI hardcodes `client_id="hermes-cli"` (`DEFAULT_NOUS_CLIENT_ID`, `auth.py:77`) — a fixed, well-known, first-party public client, not an `agent:{id}` Task 3's `RegisterSelfHosted` can mint, so `POST /api/oauth/device/code` 400s `invalid_client` on the very first call without a matching `oauth_clients` row. Fixed by `migrations/905_hermes_cli_first_party_client.sql`, which seeds `client_id='hermes-cli', kind='FIRST_PARTY', status='active', owner_user_id=0, org_id=0, redirect_uri_origin='urn:ietf:wg:oauth:2.0:oob'`. `kind='FIRST_PARTY'` is a new third value — neither `SELF_HOSTED` nor `HOSTED` fits, since both existing values denote *a gateway instance* (the schema's own doc comment) and hermes-cli is the CLI binary itself, one client_id shared by every install; introducing the value is safe because `kind` has no CHECK constraint (`902_oauth_client.sql`) and nothing in Go switches on it (verified by grep — only ent-generated boilerplate touches the field). `owner_user_id`/`org_id` are `0` as an explicit "no owner" sentinel, not a fabricated user: both columns are `NOT NULL` with no `FK` (so no schema change was forced), and `users`/`orgs` are `BIGSERIAL PRIMARY KEY` starting at 1, so `0` can never collide with a real row — see the migration's own header comment for the full reasoning the brief asked for. (2) The real CLI sends and client-side-asserts `scope=inference:invoke` (`NOUS_INFERENCE_INVOKE_SCOPE`, `auth.py:78-80`; the assertion is `_nous_invoke_jwt_status`, exact-equality, mirroring this repo's own `scopeSatisfies`), but the design doc's scope table and several tests named it `inference`. `RequestCode`/`mintAccessToken` are scope-pass-through (they echo back whatever the client sent — verified by reading `oauth_device_service.go`/`oauth_token_service.go`; no code path hardcodes a default), so this did not actually block the wire protocol in this run, but was corrected everywhere the vocabulary is defined or asserted anyway, per the brief: the design doc's scope table (`docs/superpowers/specs/2026-08-17-inferno-oauth-authorization-server-design.md`, with a note explaining the correction) and the scope literals in `oauth_handler_test.go`, `oauth_device_service_test.go`, `oauth_token_service_test.go`, `oauth_scope_test.go`, `refresh_token_cache_test.go` — so a future scope-enforcement feature (e.g. gating `/v1/*` on `inference:invoke`) is built against the vocabulary the real client actually uses, not a name nobody sends. Conformance itself: built the binary against throwaway Postgres+Redis (`t8-pg`/`t8-redis`), confirmed the seeded `hermes-cli` row and the RFC 8628 device-code response (all six required fields, unwrapped). Rather than the full interactive `oc setup` wizard (which exits immediately under this environment's non-TTY stdin, before touching the network — `hermes_cli/setup.py`'s own guard) or automating an unrelated curated-model-picker menu behind a forced pty, drove the real, unedited `hermes_cli.auth._nous_device_code_login` (plus the same `_save_provider_state`/`_save_auth_store` persistence `_login_nous` uses) directly from a small driver script — 100% real client code making real HTTP requests, scoped to exactly the OAuth mechanics under test; documented as a deliberate deviation in the runbook and report. `HERMES_HOME` was set to a scratch directory for the whole run, never the real `~/.hermes`. Approved via the API directly (bearer session from the seeded auto-setup admin, itself seeded a personal org first, same as Tasks 3/5/6/7) rather than a real browser, since Task 7 already verified the `/device` screen visually with screenshots — this run's focus was the CLI side. Verified: the driver's printed instructions never raised `Device code response missing fields`; the poll loop returned and wrote `auth.json` to the scratch `HERMES_HOME`; the persisted access token's header decodes to `alg: ES256` with a non-empty `kid`; the token's signature verifies against `GET /.well-known/jwks.json` (PyJWT, real signature check, not a shape check) with claims `aud: hermes-cli`, `scope: inference:invoke`, `sub` matching the approving user; a second, independent device authorization polled twice within one second returned `authorization_pending` then `slow_down`. Full transcript (redacted — no raw device_code/refresh_token/access_token, JWT headers only) in `task-8-report.md`; the reproducible runbook is `scripts/oauth-conformance.md`. Gate: `go test ./...` all green, `golangci-lint run --new-from-rev=36ea7ab8 ./...` 0 issues. **Task 8 fix (this addition, Critical, found in review of Task 8 itself):** the refresh_token grant was broken against the real client. `hermes_cli/auth.py:5507-5521`'s `_refresh_access_token` sends the refresh token in an `x-nous-refresh-token` HEADER — the body carries only `grant_type`/`client_id` — but `Token`'s `refresh_token` branch read `c.PostForm("refresh_token")`, which the real client never populates, so every refresh 400'd `invalid_grant`. Task 8's own conformance run had exercised device_code only and missed it; RFC 6749 §5.2 compliance and full wire-contract test coverage on the body-field path gave false confidence. This is Critical rather than a follow-up: access tokens live 15 minutes and the refresh grant is the entire mechanism behind "log in once, the agent runs forever" — broken, every agent dies 15 minutes after login and never recovers, invisibly, since the device-code leg alone looks like complete success. Fixed by `refreshTokenFromRequest` (`handler/oauth_handler.go`): reads the `x-nous-refresh-token` header first (trimmed; empty-after-trim falls through, not treated as a present-but-empty credential), then the RFC 6749 body field — so any other RFC-conformant client that only knows the body field still works. No new accepted name was invented (the brief's own instruction: the client is the specification, a second name is a second thing to keep in sync). `oauth_handler_test.go` gained `TestTokenRefreshGrantCredentialSource` (header alone, body alone, header-takes-precedence-over-a-wrong-body-value, empty/whitespace-header-falls-through, both-absent-keeps-invalid_grant) — the header case had zero prior coverage anywhere in the suite. Verified live, not just at the Go layer: obtained a token pair through the real device_code login (same driver script Task 8 used), then called the real, unedited `hermes_cli.auth._refresh_access_token` directly against the server and confirmed it returned a new, different access token with a rotated refresh token — the exact function `refresh_nous_oauth_pure` calls internally for in-process token renewal. `backend/scripts/oauth-conformance.md` gained defect 3 and a new step 8 covering the refresh leg, with an explicit "run both grants, not just device_code" note at the top — the gap this fix closes is precisely the shape of bug a device-code-only conformance run cannot see. Gate: `go test ./...` green, `golangci-lint run --new-from-rev=4fb41de5 ./...` 0 issues. **Final whole-branch fix wave (2026-08-18)** — six defects that lived in the gaps between tasks, invisible to any single task's review. (1) `EnsurePersonalOrg` had only registration-path callers, so every pre-existing user had zero `org_members` rows and `POST /api/oauth/self-hosted-client` 500'd for them; it is now called from `AuthService.GenerateTokenPair`, the single funnel every authenticated session passes through, warn-and-continue so it can never block a login. No backfill migration: the login hook repairs a user the first time they sign in, which is also the first moment they could need an org, and it additionally covers the fail-open registration call that migration 904 could not. (2) OAuth refresh tokens now carry and check `TokenVersion` (`resolvedTokenVersion`, a fingerprint of email+password_hash), so an in-app password or email change kills an agent credential the way it already killed panel sessions — previously a stolen `~/.hermes/auth.json` self-rotated for its full 30 days after the victim changed their password. (3) `oauth_client.status` is finally read: `UsableByClientID` gates `RequestCode` and BOTH token grants against an allowlist of `pending|active`, rejecting `revoked` and anything a later migration invents. `pending` stays usable until sub-project #1 ships the instance-confirmation flip, because self-hosted registration creates clients `pending` and nothing else activates them. (4) `setStatusByUserCode` only transitions a `pending` row, via a status-predicated UPDATE, closing `denied→approved` and `expired(consumed)→approved` — the latter let one human approval mint two token pairs. (5) Requested `scope` is validated against a fixed vocabulary (`invalid_scope` on anything else) and the approval screen now shows the requesting client and a human-readable permission list before Approve is offered at all, per RFC 8628 §5.4. (6) `GET /api/oauth/account` was reshaped to the keys `hermes_cli/nous_account.py` actually reads (`user`, `organisation`, `paid_service_access`); `subscription`, `tool_access` and `privy_did` are deliberately omitted and `paid_service_access` is an explicit no-paid-tier stub, since mapping Inferno's billing model onto Nous's is deferred. Also folded in: rune-based (not byte-based) client-name length so CJK names are not wrongly rejected, and a fourth `success` tone on `InterstitialState` so an approval no longer renders in the same amber caution card as an error. **New plan, Task 1 (2026-08-18): RS256 migration.** The gateway-brokered login the Hermes Desktop client actually uses (`plugins/dashboard_auth/nous/__init__.py:444` in the read-only client repo) hard-codes `algorithms=["RS256"]` and rejects our ES256 tokens outright, so the signing key moves to RSA before the authorization_code + PKCE flow is built on top of it — one algorithm in the tree, never an interim state with both live. `SigningKey.Private` is now `*rsa.PrivateKey` (`rsa.GenerateKey(rand.Reader, 2048)`, PKCS1 PEM `RSA PRIVATE KEY`); the security_secrets row moves from `oauth_es256_active` to `oauth_rs256_active` (`legacyES256SecretName` kept as a comment-only constant so migration 907 can name the row it deletes); `kidFor` now hashes the DER of `x509.MarshalPKIXPublicKey` instead of the EC point, same `base64.RawURLEncoding` first-16-bytes-of-SHA-256 shape. `JWKS` emits `kty: RSA`, `n`/`e` (b64url, `e`'s leading zero bytes stripped so 65537 renders as the canonical `AQAB` rather than an accepted-by-some/rejected-by-others padded form) — the six private RSA fields (`d`,`p`,`q`,`dp`,`dq`,`qi`) are asserted absent, superseding ES256's single-field `d` check. New `ErrUnknownKid` + `ByKid(ctx, kid)` is the ONLY lookup `RequireOAuthScope` uses now (`Active()` is no longer called from the middleware at all) — today it still just resolves the one active key, but routing verification through it is what lets a second key be added later (rotation) without touching the middleware. A token with no `kid` header is rejected outright, not resolved to the active key. The concurrency-safe create path (lost-race re-read via `dbent.IsConstraintError`) is unchanged in shape, only the key type changed; `TestActiveIsConcurrencySafe` still passes untouched. `mintAccessToken` switches to `jwt.SigningMethodRS256` and adds two claims the gateway's verifier reads: `oauth_contract_version: 1` and `agent_instance_id` (the `client_id` suffix after `agent:`, cross-checked by the client against its own configured client_id as defense-in-depth). `RequireOAuthScope`'s parser allows `RS256` only and resolves the verification key by the token's own `kid` header via `ByKid`, not `Active()`. Migration `907_oauth_rs256_key.sql` deletes the `oauth_es256_active` row so the service mints a fresh RSA key on first use — safe because, per the plan brief, nothing has ever depended on a live ES256 token: `oauth_signing_key.go` doesn't exist on the parent branch and the running container was upstream's published image, so no token was ever issued outside throwaway test containers. Every ES256/`kty: EC`/`crv: P-256`/`x`,`y` assertion in `oauth_signing_key_test.go` and `oauth_scope_test.go` was replaced with its RSA equivalent rather than deleted; `oauth_scope_test.go`'s algorithm-rejection test now additionally asserts an ES256-signed token (the exact shape of a stale pre-migration token) is rejected, not just HMAC. **New plan, Task 2 (2026-08-18): `redirect_uri` validation.** `oauth_client.redirect_uri_origin` was accepted from the caller and stored completely unvalidated (the OAuth AS spec's own "What was NOT built" gap, closed by D-3 of the authorization_code + PKCE design doc). New `service/oauth_redirect_uri.go` holds two exported validators plus a bridge, deliberately not one function behind a boolean flag, because registration and `/oauth/authorize` (Task 4) enforce genuinely different rules on genuinely different shapes: `redirect_uri_origin` stores an ORIGIN — every existing registration fixture passes a bare origin like `https://agent.example.com`, and the DTO field is literally named `redirect_origin` (`dto/oauth.go`) — so `ValidateRedirectOrigin` requires https/host/no-userinfo/no-path/no-query/no-fragment/not-loopback and nothing about `/auth/callback`; `ValidateRedirectURI` adds the `/auth/callback` path-suffix requirement on top, for the full redirect URI Task 4 will validate. `RedirectURIMatchesClient` bridges them: PARSED scheme+host equality (net/url's `Host` field already includes an explicit port) — never `strings.HasPrefix` or any substring check, since prefix matching is exactly what would accept `https://agent-abc.tryopencomputer.com.evil.com` against a registered `https://agent-abc.tryopencomputer.com`, and exact-Host-equality is also what catches a port mismatch (`https://host:8443` vs `https://host`) that a scheme+hostname-only comparison would miss. Loopback rejection covers all three forms in one `isLoopbackHost` helper — `127.0.0.0/8` via `net.ParseIP(...).IsLoopback()` (not a `127.0.0.1`-only string check), `::1` via the same call once `net/url` has stripped the host's brackets and port, and the literal name `localhost` via case-insensitive comparison. Migration 905's seeded `hermes-cli` row (`redirect_uri_origin = 'urn:ietf:wg:oauth:2.0:oob'`, the conventional OAuth placeholder for a redirect-less client) is a deliberate, migration-inserted exception, not drift: `ValidateRedirectOrigin` validates INPUT at registration time only and is never run retroactively against stored rows, and the new `IsOOBOrigin` predicate is exported so Task 4's authorize handler can reject that client from the authorization_code flow with its own explicit error before ever reaching `RedirectURIMatchesClient` — falling through to origin matching would instead fail with a misleading "redirect_uri did not match" for a client that never had one. Enforced at registration in `RegisterSelfHosted` (`oauth_client_service.go`), before the insert, returning the same `ErrInvalidRedirectURI` sentinel `oauth_redirect_uri.go` defines; `oauth_handler.go`'s `RegisterSelfHostedClient` maps it to a bare `400 {"error":"invalid_request"}` alongside the pre-existing `ErrClientNameTooLong` mapping, one `errors.Is` check, not a second branch. No existing registration fixture needed to change — grep confirmed every one already passes an origin-shaped `https://` value with no path, none loopback, none plaintext http. Gate: `go test ./...` green, `golangci-lint run --new-from-rev=0c4bae2b ./...` 0 new issues. **New plan, Task 3 (2026-08-18): authorization codes -- issue and redeem.** The security core of the flow: a new `oauth_authorization_codes` table (migration `906`, column set and order cross-checked against the generated `ent/migrate/schema.go` rather than written from memory, after a prior task's review caught exactly that drift once already) plus two new pieces -- `OAuthAuthorizeService.IssueCode` (a new file, `oauth_authorize_service.go`) which validates and persists a `pending` code, and `OAuthTokenService.ExchangeAuthorizationCode` (added to the existing `oauth_token_service.go`) which redeems one. `IssueCode` rejects `code_challenge_method=plain` outright (`ErrPlainChallengeMethodRejected`) -- RFC 7636 permits it but it provides no protection, so this server only ever issues S256 codes -- validates the requested scope against the same closed vocabulary `RequestCode` uses, resolves the client via `OAuthClientService.UsableByClientID`, rejects an OOB (device-flow-only) client's origin via `IsOOBOrigin` before it can reach `RedirectURIMatchesClient` (Task 2), and mints the code as 32 bytes of `crypto/rand`, hex-encoded, with its `Read` error checked -- a predictable code is a free login. `ExchangeAuthorizationCode` follows the device grant's status-predicated CAS shape (`UPDATE ... WHERE code = ? AND status = 'pending'`) but applies it FIRST, before any of the PKCE/client_id/redirect_uri/expiry checks that follow, not after: the property that matters is that no two concurrent presentations of the same code can both reach the minting call, and validate-then-consume is exactly the read-then-write race that took two review rounds to close on the refresh-token path (`MarkRotated`) -- claiming the row first closes it unconditionally, at the cost of a code with a wrong PKCE verifier or mismatched client also being permanently burned on its first (failed) presentation, a deliberate fail-safe against unlimited guessing rather than a bug. PKCE verification hashes the presented `code_verifier` (SHA256, then `BASE64URL-NOPAD`) and compares it to the stored `code_challenge` via `subtle.ConstantTimeCompare`, never `==`, so a forged verifier cannot be narrowed by response-timing. `client_id` and `redirect_uri` are checked for exact equality against what was bound at issue -- the classic authorization-code interception vector RFC 6749 §10.5 exists to close. Every redemption failure -- unknown code, replay, wrong verifier, wrong client, wrong redirect_uri, expired -- collapses to the single `service.ErrInvalidGrant` sentinel, same anti-probing reasoning the other two grants already apply to their own failure sets. The replay case (`affected == 0` because the row was already `consumed`) is the one the review was told to check hardest: RFC 6749 §4.1.2 requires a second presentation of a code to revoke whatever the first one already minted, since the server cannot tell which of the two arrivals is the attacker. `issued_token_family` is therefore recorded on the row (a second, best-effort-but-fatal-on-failure `UPDATE`) only once tokens have actually been minted from it -- nil if the consuming presentation itself failed validation, since nothing was ever issued from it for a later replay to revoke -- and a replay with a non-nil family calls `RefreshTokenCache.DeleteTokenFamily` on it before returning `ErrInvalidGrant`. `TestAuthorizationCodeIsSingleUseAndReplayRevokes` asserts both that the replay itself fails AND that the refresh token the FIRST redemption minted is independently dead afterward (`ExchangeRefreshToken` on it also returns `ErrInvalidGrant`) -- mutation-tested by temporarily disabling the revocation call and confirming that exact assertion, and only that assertion, then fails; a test checking only the replay's own failure would pass against an implementation that merely marks the code consumed and leaves the stolen tokens live, which is exactly the weaker shape the refresh-token reuse test on this same branch had to be strengthened away from once already. `Token`'s switch in `oauth_handler.go` gained an `authorization_code` case reading `code`/`redirect_uri`/`client_id`/`code_verifier` from the form, following the refresh branch's error shape exactly: `ErrInvalidGrant` maps to `400 {"error":"invalid_grant"}`, everything else is logged and returned as `500 server_error` rather than folded into an OAuth error code. `knownScopes` gained `ScopeAgentDashboardAccess = "agent_dashboard:access"`, sent by the gateway on every authorize request for the Hermes Desktop login leg -- nothing consumes it and its security value is zero, but omitting it would 400 `invalid_scope` on every desktop login via `ValidateScope`'s whole-request rejection, so it is echoed faithfully. Gate: `go test ./...` green (including `-run TestAuthorizationCode -v`, all 7 tests from the brief), `golangci-lint run --new-from-rev=62d124d1 ./...` 0 new issues. **Task 3 review fixes (2026-08-18, three Importants, no Criticals; the review verified the CAS race closure down to ent v0.14.5's `sqlgraph.UpdateNodes` source, confirming the no-edges schema takes the single-statement `UPDATE` branch).** (1) An internal failure downstream of the consuming CAS -- a signing-key store blip, a Redis hiccup in `issueRefreshToken` -- previously left the code permanently `consumed` with nothing minted, forcing a full browser re-login for a fault that had nothing to do with the caller's credential (and an HTTP client's default 5xx retry would immediately hit the replay path on its own). Fixed by `rollbackConsumedCode`, called ONLY on the internal-error branches (row re-query failure, a non-`ErrUserNotFound` user-load failure, family-id entropy failure, the family-recording update, `mintAccessToken`, `issueRefreshToken`) and deliberately NOT on any credential-rejecting validation branch (wrong PKCE verifier, mismatched client/redirect_uri, expiry, a revoked client, a genuinely missing/inactive user) -- rolling those back would let a guessing attacker retry one code indefinitely, defeating the original consume-first design. Safe by construction: only the single goroutine that won the original CAS can ever reach an internal-error branch, so a rollback never races a concurrent redemption. (2) `issued_token_family` is now recorded on the row immediately after `newFamilyID()`, BEFORE `mintAccessToken`/`issueRefreshToken` run, not after: the prior order left two windows where a replay could not be revoked -- if the recording update itself failed after a successful mint, the resulting family was unrevocable forever, and even when it succeeded, a replay landing between the mint completing and the record committing read `IssuedTokenFamily == nil` and skipped revocation, defeating RFC 6749 §4.1.2 for that window. Recording first is strictly safer: a family recorded but never actually minted under (an internal failure after this point, now rolled back) is harmless, since `DeleteTokenFamily` on a family nothing was ever stored under is a no-op. (3) `IssueCode`'s empty-`code_challenge` rejection wore `ErrInvalidRedirectURI`, the wrong sentinel: RFC 6749 §4.1.2.1 requires the authorization server to distinguish redirect_uri-untrustworthy errors (render an error page, never redirect) from every other rejection (redirect back with `error=...`), and Task 4 will branch on `ErrInvalidRedirectURI` to choose the first arm -- so a client that merely omitted `code_challenge` would have gotten an opaque error page instead of the redirect its state machine expects. Replaced with a new `ErrMissingCodeChallenge` sentinel, and folded in Minor-8 at the same call site: `validCodeChallengeShape` now checks the RFC 7636 §4.2 fixed shape of an S256 challenge (exactly 43 unpadded base64url characters), turning a malformed challenge from a silent `invalid_grant` several steps later at redemption into an immediate, diagnosable `invalid_request` at issue -- it failed closed either way, this only makes the failure legible. Every other `IssueCode` rejection was checked against the same sentinel-choice question the review asked and found correctly categorized already (scope keeps `ErrInvalidScope`; the OOB and origin-mismatch checks are genuine redirect_uri problems). Folded in three Minors: (4) `handleAuthorizationCodeReplay` now gates revocation on `row.ClientID == clientID`, not just `IssuedTokenFamily != nil` -- without this, a registered client B that merely observed client A's leaked code (proxy access logs, `Referer`, browser history all carry it in a query string) could revoke A's freshly-minted family by replaying it under B's own client_id, a self-inflicted denial-of-service A never triggered; the unauthenticated-replay posture RFC 6749 requires is preserved (the wire response is `ErrInvalidGrant` either way, so this reveals nothing about which client_id actually owns the code). (7) doc comment on `handleAuthorizationCodeReplay` now says explicitly that the access token minted by a replayed redemption survives revocation for the rest of its 15-minute TTL -- inherent to a stateless, self-verifying JWT with no server-side record to delete, the same property `ExchangeRefreshToken`'s own reuse-detection path already lives with, not a new gap. Deferred exactly as scoped by the review: Minor 5 (the 500-vs-400 timing oracle during a Redis outage), Minor 6 (`assertClientUsable` folding into `ErrInvalidGrant`, pre-existing across all three grants and wanting its own pass), Minor 9 (no reaper for expired codes), Minor 10 (pre-existing duplicate DECLARED paths and the unrelated Razorpay divergence noted in Task 3's own report). New tests: `TestAuthorizationCodeInternalFailureRollsBackAndFamilyIsRecordedBeforeMint` (a `failOnceRefreshTokenCache` test double injects one transient store failure; asserts the row is back to `pending` with `issued_token_family` already non-nil, then that the SAME code redeems successfully on retry -- mutation-tested by disabling `rollbackConsumedCode`'s body and confirming the status-reverted assertion, and only that assertion, then fails) and `TestAuthorizationCodeReplayRevocationIsGatedOnClientID` (a second, unrelated registered client replays client A's already-consumed code; asserts the replay itself is rejected AND that A's legitimate refresh token is still live afterward). Gate: `go test ./...` green, `golangci-lint run --new-from-rev=34cb807d ./...` 0 new issues. **Task 3 review round 2 (2026-08-18, four Importants, no Criticals; the re-review traced the `consumed -> pending` transition down to proof that a rollback can never trail a later successful redemption, and empirically fuzzed the 43-char S256 shape check over 500,000 random verifiers with zero false rejections).** (1) `assertClientUsable`'s branch folded EVERY error into `ErrInvalidGrant` without rolling back, including raw ent errors from a database fault (`OAuthClient.Query().Only(ctx)` returns more than just `ErrClientNotUsable`) -- reopening the exact I1 failure mode one call site over. Fixed by splitting on `errors.Is(err, ErrClientNotUsable)`: a genuine policy revocation stays `ErrInvalidGrant` with no rollback (a one-way door, as intended); anything else rolls back and surfaces as a logged 500. (2) `rollbackConsumedCode` issued its UPDATE on the caller's own `ctx` -- which the handler passes as `c.Request.Context()`, canceled on client disconnect or request timeout, precisely the fault class the rollback exists to recover from. Fixed with the codebase's existing detached-context idiom (`context.WithoutCancel` + a bounded `rollbackConsumedCodeTimeout` of 5s, mirroring `accountRepository.syncSchedulerAccountSnapshotDetached` and `systemUpdateContext`). (3) The mutation tests only proved the rollback fires somewhere, not that it never fires on a credential-rejecting branch -- a mutant adding `rollbackConsumedCode` to the PKCE-mismatch branch passed all 9 existing `TestAuthorizationCode*` tests, making a wrong-verifier attempt infinitely retryable. `TestAuthorizationCodeRejectsWrongVerifier` now re-reads the row (asserts `status=consumed`) and retries with the CORRECT verifier (asserts it STILL fails) -- mutation-tested by adding the rollback to that branch and confirming the new assertion, and only that assertion, fails, then reverting. (4) `IssueCode`'s unknown/unusable-`client_id` rejection had no sentinel Task 4 could branch on -- the mirror image of round 1's finding 3: RFC 6749 §4.1.2.1 puts an invalid `client_id` in the same MUST-NOT-redirect class as an untrustworthy `redirect_uri`, so a Task 4 handler written to this file's own "branches on `ErrInvalidRedirectURI`" guidance would fall through to the redirect arm for an unknown client and bounce the browser to a `redirect_uri` never validated against any registration -- an open redirect. Fixed with a new `ErrUnknownClient` sentinel, required to land before Task 4 is written. Folded in one Minor: `rollbackConsumedCode` now also calls `ClearIssuedTokenFamily()` -- without it, a stale family survives the revert (harmless in itself, since a later successful retry overwrites it) but a replay landing in the narrow retry window would log a misleading "replay detected, revoking issued token family" and call `DeleteTokenFamily` on a family nothing was ever stored under. This made the family-recorded-before-mint ordering test (round 1) unobservable post-call (the row looks the same, family nil, whether recorded-then-cleared or never recorded), so that test now asserts via an `onFail` hook on the fake refresh-token cache firing mid-flight, at the exact moment between the family-recording update and the injected failure. Two new tests beyond the explicit fixes: `TestIssueCodeRejectsUnknownClient` and `TestAuthorizationCodeRejectsRevokedClientWithoutRollback` (proves finding 1's other half -- a genuine policy revocation must still leave the code burned, not just avoid the DB-fault misclassification). Deferred exactly as scoped: the `issueRefreshToken` partial-store orphan (pre-existing, shared by all three grants), `ErrPlainChallengeMethodRejected`'s name being narrower than its condition (cosmetic), and the residual burn window on the initial CAS's own `Save` failure (unavoidable without reintroducing the resurrection bug the reviewer proved impossible under the current design). Gate: `go test ./...` green, `golangci-lint run --new-from-rev=b8aeb2aa ./...` 0 new issues. **Task 3 review round 3 (2026-08-18, two Importants, no Criticals; the re-review verified F2's context-detachment has no goroutine leak since all six rollback call sites are synchronous, confirmed the cited `account_repo.go` precedent is character-for-character the same shape, verified F1 at the handler -- the sentinel comparison is exact and the fault half reaches the logged-500 default arm -- and proved `ClearIssuedTokenFamily` revokes zero real capability since the only rollback site with live cache state, `issueRefreshToken` failure, means `DeleteTokenFamily` would enumerate an empty family set anyway).** (1) `IssueCode`'s client-lookup error wrap used `%v` for the inner `UsableByClientID` error, destroying the cause chain -- the exact misclassification F1 (round 2) was raised about, one file over: a transient DB fault inside `OAuthClient.Query().Only(ctx)` surfaced identically to "client never registered" (`ErrUnknownClient`), so Task 4 would render an unknown-client page during a database outage instead of a 500, hiding the outage from alerting. Fail-closed (no redirect either way), so not a security hole, but the same mistake. Fixed by wrapping with a SECOND `%w` instead of `%v` (Go 1.20+ supports multiple `%w` per `fmt.Errorf` call), so Task 4 can `errors.Is(err, ErrUnknownClient)` to decide never-redirect and separately `!errors.Is(err, ErrClientNotUsable)` to decide 500-vs-error-page without a second round trip; revoked-vs-unknown stays deliberately collapsed on the wire. (2) The wrong-branch mutation coverage only spanned 2 of 6 credential-rejecting branches (PKCE mismatch, revoked client) -- the other four (foreign client, redirect_uri mismatch, expired, user missing/inactive) had no row re-read, so a rollback added to any of them passed the suite. The expired case was actively misleading: its existing assertion (`IssuedTokenFamily == nil`) is SATISFIED by the very mutant it should catch, since a rollback also clears the family. Extracted a shared `authCodeFixture.assertCodeStillConsumed` helper (re-reads the row, asserts `status == consumed`) and wired it into the redirect-mismatch, foreign-client, and expired tests (plus refactored the wrong-verifier test to use it, removing duplicated inline logic); the expired test now asserts BOTH properties explicitly, with a comment explaining why the family-only check alone cannot discriminate correct behavior from the bug. Mutation-tested on the expired branch specifically (the one the review called instructive): added `rollbackConsumedCode` to the `time.Now().After(row.ExpiresAt)` branch, confirmed `TestAuthorizationCodeExpires` fails exactly at the new status assertion, reverted. User-missing/inactive (`:646`/`:668`) remains uncovered by design -- not in the review's explicit action list ("call it from the redirect-mismatch, foreign-client and expired tests"), noted as a residual gap in the round-3 report rather than silently left unremarked. Folded in one timeout tightening: `rollbackConsumedCodeTimeout` 5s -> 2s, matching the `account_repo.go` precedent -- the rollback runs on the synchronous request path, so a longer bound would hold a request goroutine and a pool connection for the full duration on every failed redemption during a database outage. Gate: `go test ./...` green, `golangci-lint run --new-from-rev=7959c502 ./...` 0 new issues. Task 6 (conformance against the real gateway plugin, authorization_code+PKCE plan): drove the unedited `plugins/dashboard_auth/nous` provider from the read-only client repo (`/Users/saksham/OpenComputerV2/OpenComputerV2`) through `start_login` -> browser leg -> `complete_login` -> `verify_session` -> `refresh_session` -> grace replay -> code replay, against a live server built with `-tags embed` and a real `inferno-frontend` dist -- the production build shape, in which `FrontendServer.Middleware()` runs before route registration and can swallow `/oauth/authorize` and `/.well-known/jwks.json`. Both routes verified to reach their handlers under that build. Two server defects found, both invisible to every unit test because each depends on a config value the tests always pass in canonical form: (1) HIGH -- `NewOAuthTokenService` stored `cfg.Server.FrontendURL` verbatim as the `iss` claim while `NewOAuthDeviceService`, handed the SAME value, has always normalised it (`strings.TrimRight(portalBaseURL, "/")`); a trailing slash (accepted by config validation, and the shape a browser address bar yields) produced `iss: "https://portal/"`, which the client rejects on EVERY token because it rstrips `/` before PyJWT's `issuer=` check -- the whole dashboard grant dead from one character. Fixed by normalising in the constructor. (2) MEDIUM -- with `SERVER_FRONTEND_URL` unset the device grant refused loudly (`ErrPortalNotConfigured`) but the authorization_code/refresh grants minted tokens carrying `iss: ""`, so the failure surfaced on the CLIENT as "Invalid issuer" with nothing in our logs, pointing operators at the agent's config instead of the server's. Added `ErrIssuerNotConfigured` and a refusal in `mintAccessToken` (the single point that stamps the claim, so no grant routes around it); deliberately NOT mapped to an OAuth error code -- it takes the handler's existing logged-500 `server_error` arm rather than telling every agent its credential is dead. Regression guards `TestIssuerTrailingSlashIsNormalisedIntoTheIssClaim` (asserted on the emitted CLAIM, not the struct field) and `TestMintRefusesWhenIssuerIsNotConfigured` in `service/oauth_token_service_test.go`. Documented-not-fixed: a refresh issued within the same second as the login returns a byte-identical access token (no `jti`, one-second `iat`/`exp` resolution, deterministic RS256/PKCS#1 v1.5) -- harmless, and adding a claim is a token-contract change a conformance task should not make. `scripts/oauth-conformance.md` gains the dashboard leg as step 9 (including the mandatory embed-build probes and a failure-mode-to-cause table) and has its stale `ES256` expectations in steps 1/5/6 corrected to `RS256`, which Task 1 of this plan had migrated without updating the runbook. FINAL WHOLE-BRANCH FIX WAVE (closes the SHIP-WITH-CONDITIONS conditions in `.superpowers/sdd/2026-08-18-authorization-code-pkce/final-review.md`): `migrations/908_oauth_authorization_code_client_user_idx.sql` -- a PARTIAL index on `oauth_authorization_codes (client_id, user_id) WHERE status = 'pending'`, covering Task 4's F9 supersession UPDATE, which runs on every `/oauth/authorize` issuance and had no index but `status`, whose selectivity inverts as the table fills with `consumed` rows (M-8's index half; the missing reaper stays a separate operational task, and the index is SQL-only because ent's index builder cannot express a partial index and production migrates from `migrations/`, not from ent automigrate). Also in this wave, all in files already declared above: C-1 -- `ExchangeDeviceCode` and `ExchangeRefreshToken` now split `ErrClientNotUsable` from an infrastructure fault at `assertClientUsable`, as `ExchangeAuthorizationCode` already did, so a Postgres blip is a logged 500 rather than an `invalid_grant` the real client answers by silently erasing its stored OAuth material. I-5/I-3 -- `RefreshTokenCache` gains `PersistRefreshToken`, one Lua `EVAL` writing the record and BOTH revocation-index memberships together and refusing under a `family_revoked:{id}` marker that `DeleteTokenFamily` now writes BEFORE it enumerates; this replaces three non-atomic writes that could leave a live 30-day credential outside every revocation index (and that the Task 5 grace then handed to the client on its retry), and closes the SMEMBERS-vs-SADD race that let a concurrent rotation re-create a family just revoked for reuse. `DeleteUserRefreshTokens` now SREMs the hashes it enumerated instead of DELing the set key, and both delete paths SREM from the owning user's set (M-5). I-2 -- `rollbackConsumedDeviceCode` gives the device grant the rollback the authorization_code grant has, so an internal failure after the consuming CAS no longer burns a human's approval and reports `expired_token` for a code with ten minutes left. I-4 -- `IssueCode` enforces the `billing:manage` refusal that only the handler enforced. M-4 -- `RequireOAuthScope` now verifies `iss` (via `jwt.WithIssuer`, plumbed from `OAuthTokenService.Issuer()` so the verifier cannot drift from the minter) and `aud` (required, single-valued, and resolved against the client registry, so a revoked client's access tokens die at once instead of living out their 15 minutes); an unset issuer fails closed with a 500. M-3 -- the `Bearer` scheme match is now case-insensitive per RFC 6750 s2.1. M-6 -- `/oauth/authorize/` (trailing slash) joins the embed bypass allowlist. T6-1/T6-2 -- two overstated comments corrected, and the issuer-guard test gains the authorization_code grant it claimed to cover. Frontend (not scanned by this script, which reads `frontend/` and not `inferno-frontend/`): `inferno-frontend/src/api/client.ts` -- M-9, an error body that arrived as a string because the request set `responseType: 'text'` is now JSON-parsed, so `/oauth/authorize`'s 429 envelope is readable; M-10, the failed-refresh bounce to `/login` now carries `?redirect=` built from `window.location`, so a session expiring on the consent screen no longer discards the whole authorization request. **oauth-bearer-inference Task 3:** `OAuthBackingKeyService.Resolve(ctx, userID, clientID)` returns the `api_keys` row with `User` and `Group` eager-loaded, creating it on first use. A row has to exist because there is no keyless inference path: `usage_logs.{user_id,api_key_id,account_id}` are all NOT NULL with `usage_logs_api_key_id_fkey -> api_keys(id)`, `usage_billing_dedup.api_key_id` is NOT NULL and half of `UNIQUE (request_id, api_key_id)`, the quota/rate-limit ledger IS the key row, and `apiKey.Group` is the only input to routing/pool selection/model mapping/pricing. The secret comes from `GenerateAPIKeySecret`, the same generator ordinary keys use (extracted from `APIKeyService.GenerateKey`, which now delegates to it) so a hypothetical leak is bounded by the ordinary-key blast radius -- and nothing returns or logs it: there is no accessor for it, and `scrubBackingKeySecret` strips it from any error string before that error travels, because a Postgres unique violation puts the offending value in its DETAIL line. Concurrency: migration 909's partial unique index on `(user_id, oauth_client_id)` is the serialization point -- two simultaneous first requests both miss the lookup, one INSERT wins, and the loser turns the constraint error into a re-read of the winner's row rather than a 500. Group policy: `oauth_backing_key.group_name` is matched by name the way the platform's other default groups are (`repository.createGroupIfNotExists`); there is deliberately NO default value, because unconfigured must mean refuse-with-`ErrNoGroupForOAuthKey` (Task 4's 403) rather than bind an agent to whichever group sorted first. The key is still registered with viper so its env var is reachable (`internal/config/env_reachability_test.go` fails any key that is not). A database failure while resolving the group is deliberately NOT `ErrNoGroupForOAuthKey` -- infrastructure faults surface as 500. Nothing here deletes a backing row: `usage_logs_api_key_id_fkey` is ON DELETE CASCADE, so a hard delete would silently erase that agent's whole usage history. **Task 3 fix round 1 (review: spec OK, quality ship-with-conditions, 9 findings).** F1 (High) was user-reachable, not merely latent as the first report claimed: `APIKeyService.Delete` authorized on ownership alone, and a backing row IS owned by the OAuth user, so any user who learned their backing row id could tombstone it -- and because 909's index did not filter `deleted_at` while ent's soft-delete interceptor filters every read, the tombstone then held the `(user_id, oauth_client_id)` slot forever and the agent could never resolve again. Fixed in two halves: migration 910 recreates the index with `AND deleted_at IS NULL` so the state is recoverable (909 is not edited -- applied migrations are checksum-verified), and `APIKeyService.Delete` now returns `ErrOAuthBackingKeyUndeletable` for any row with a non-NULL `oauth_client_id` so the state is not reachable. That guard needed `oauth_client_id` on the domain struct, so `service.APIKey` gains `OAuthClientID` and `apiKeyEntityToService` maps it; `Delete` reads through `GetByID` instead of `GetKeyAndOwnerID` because the decision needs the column. **The review's F3/F4 premise turned out to be half wrong, and the correction changed the fix.** The claim was that PostgreSQL puts the offending value in a unique violation's DETAIL line, so `err.Error()` carries the credential. Probed against real PostgreSQL 18 + lib/pq: `err.Error()` and `%+v` render only Severity+Message and do NOT contain the key, but `pq.Error.Detail` does -- `Key (key)=(sk-...) already exists.` -- on an exported field that travels up every wrapped chain. So the original text-only scrub could never have seen the leak it was written for, while `zap.Any`/`%#v`/a JSON error reporter would have printed it in full. `scrubBackingKeySecret` is therefore replaced by `sanitizeBackingKeyError`, which FLATTENS the error (returns a plain `errors.New`, so no driver error value escapes) as well as redacting the text. F4's other half: `Resolve` now blanks `api_keys.key` on every row it returns, because `*dbent.APIKey.String()` writes `key=<secret>` and the field is `json:"key"` -- ent's `.Sensitive()` was deliberately NOT used, since ordinary key creation must still return its secret to its owner once. F2: the infrastructure-fault claim is now tested at the branch it names (the old test closed the client, so `lookup` failed first and `policyGroup` was never reached). F5: the fast path is kept and pinned by a resilience property rather than a mirror test -- a later-broken group policy must not 403 agents that already have a backing row; the first report's "equivalent mutant" verdict is corrected. F8: a row whose group is DEACTIVATED is now rebound like one whose group was deleted (the status is already on the loaded edge, so it costs no query), while a deliberate operator binding to another ACTIVE group is left alone. F6/F7/F9: no test prints a credential even on failure, `oauth_backing_key.group_name` is documented in `deploy/config.example.yaml` -- NOT `backend/config.yaml`, which `.gitignore` line 111 excludes, so an edit there is invisible to every other checkout and to the divergence script -- so the feature does not ship dark, and the nil-service and non-positive-user-id guards are tested. **Task 4 (`/v1` OAuth credential branch).** `middleware.OAuthOrAPIKeyAuth` replaces `gin.HandlerFunc(apiKeyAuth)` on the `/v1` group and on the three root-level aliases the gap evidence reproduced against (`POST /responses`, `POST /responses/*subpath`, `POST /alpha/search`); `/v1beta` is an explicit non-goal (different middleware). It REPLACES rather than wraps because the API-key path's 256-byte `Authorization` cap aborts a ~628-byte access token before any lookup, so the JWT shape test has to run ahead of that middleware (evidence finding F-A -- the reproduction's `latency_ms: 0`). The cap itself is untouched. A JWT-shaped credential that fails verification is rejected, never retried as an API key (F-B), and nothing on the OAuth branch touches the IP-keyed invalid-auth abuse limiter. 401 `{"error":"invalid_token"}` is reserved for the one failure a token refresh can fix; insufficient scope, a missing group policy, a disabled backing row and a deactivated owner all answer 403, because the real client force-refreshes on 401 and only on 401. `APIKeyEntityToService` had to be exported from `repository`: `OAuthBackingKeyService.Resolve` returns the ent entity while the `/v1` pipeline reads `*service.APIKey`, and the alternatives were a second copy of the group's ~60-field conversion or a second query per request that would also have loaded the deliberately-blanked backing secret back out of the database. **Fix round 1 (review findings F1-F11 + escalation 3)** touched no new files: `api_keys.ip_whitelist`/`ip_blacklist` are now compiled and enforced on the OAuth branch (they were displayed by the panel and inert, because only `APIKeyService.Delete` refuses backing rows, not `Update`); `/v1/usage` and `/v1/sub2api/billing` now require `billing:read` rather than being reachable with `inference:invoke` by virtue of sharing a group-level mount; a subscription-type backing group now loads and publishes its subscription, without which `CheckBillingEligibility` silently rerouted a paying customer to wallet billing and 403'd them `INSUFFICIENT_BALANCE`; a caller that has already gone away gets 499/504 instead of gin's default `200 OK` with an empty body; the branch also mounts on the root-level `/chat/completions`, `/embeddings`, `/models` and `GET /responses` aliases, because `NOUS_INFERENCE_BASE_URL` bypasses the client's host allowlist and gets no `/v1` normalisation, so the previous boundary made a total silent 401 outage depend on a config string ending in four characters. `service/oauth_signing_key.go` memoises the parsed RS256 key keyed by its stored PEM -- `ByKid` -> `Active` re-parsed an RSA-2048 private key on EVERY OAuth request (measured 132.8us, against ~30us for the verify the request actually needs; now 2.7ns). Keying by the bytes rather than by `kid` is the safety argument: the `security_secrets` SELECT is deliberately still made on every call, so a rotated row misses the cache immediately and a retired key can never be served. Caching the SELECT too is left as a named residual, because there is no rotation event in this tree to invalidate on. | **Do not hand-merge the `ent/` files.** Re-run `go generate ./ent` and let codegen rebuild from the four schema files; then re-run `cd backend/cmd/server && go generate ./...` to regenerate `wire_gen.go` from `wire.go`. Only `ent/mutation.go` and `ent/runtime/runtime.go` realistically conflict, for the same reason as D1. Tasks 2, 3, and 4's non-ent files are ordinary Go source and merge normally. **Billing contract adapter (2026-08-19 plan, Task 1):** `service/billing_contract.go` + `service/billing_contract_test.go` + `handler/billing_contract_handler.go` + `handler/billing_contract_handler_test.go` + `server/routes/billing_contract.go` + `server/routes/billing_contract_route_test.go` (+ `handler/handler.go`, `handler/wire.go`, `service/wire.go`, `server/router.go`, `cmd/server/wire_gen.go`, all already listed above) — `GET /api/billing/state`, the Nous-shaped billing overview the hermes CLI reads. A TRANSLATION LAYER over services that already exist: no new table, no migration, no new billing concept. Mounted at `/api/billing` on the server root, bare JSON (never the panel envelope), behind `RequireOAuthScope` with NO required scope — the same gate `/api/oauth/account` uses. It was `billing:read` in the design; that is a scope no token can carry (the client requests `inference:invoke`, its only step-up asks for `billing:manage`, and our AS adds no defaults), so gating on it made the endpoint dead rather than protected. Authentication is unchanged — signature, issuer, audience and expiry are still verified — and WRITE endpoints keep `billing:manage`. Composes the CACHED services (`BillingCacheService.GetUserBalance`, not a UserRepository read: this endpoint is polled by every running agent). `card`, `auto_reload` and `chargePresets` are reported ABSENT rather than invented — Inferno has no card vault, no auto top-up and no server-side preset ladder. `bounds.limitUsd` is null: per-org monthly ceilings are not modelled. `spentThisMonthUsd` is real, aggregated from `usage_logs.actual_cost` (the column the wallet is actually debited by). Also declared: `docs/superpowers/plans/2026-08-19-billing-contract-adapter.md` under D4 — the sibling spec was declared when it landed, the plan was not, and it had been undeclared since ec729664. `server/api_contract_test.go` (D2) loses one stale `"Sub2API"` expectation 05289268 left behind on the settings-default path, which only `go test -tags unit ./...` compiles. **Task 2 (same plan):** same six files, no new ones — `GET /api/analytics/usage`, the caller's own usage history, composed from `UsageService.ListByUser` (`BillingUsageSource` grows a `ListByUser` method alongside `GetStatsByUser`). Mounted at `/api/analytics/usage` on the same NO-required-scope gate as `/api/billing/state` (Ruling R-2.1 — the design doc's Step 6 said `billing:read`; superseded for the same reason `/api/billing/state`'s F2 was). Also fixes a cross-branch inconsistency this task surfaced (Ruling R-2.2): `server/middleware/oauth_inference_auth.go`'s `requiredOAuthInferenceScope` (already declared above, oauth-bearer-inference Task 4) gated `GET /v1/usage` and `GET /v1/sub2api/billing` on `billing:read` too — the same dead scope, on Inferno's own `/v1` surface. Both now require `""` instead, and NOT a silent fallthrough to `inference:invoke` (that would be a different bug: a token holding only `billing:read` would lose access it had before). `TestOAuthUsageAndSub2APIBillingRequireOnlyAValidToken` replaces the old (wrong) test that pinned the dead behaviour, and asserts all three token shapes (inference:invoke-only, billing:read-only, no-scope) reach 200 — which fails if `billing:read` is reinstated OR if the gate is changed to `inference:invoke` instead. Response keys are camelCase per this package's convention but, unlike `/api/billing/state`, are NOT verified against a live client parser: no function in the checked-out hermes-agent client (`hermes_cli/nous_billing.py` and siblings) calls `GET /api/analytics/usage` on its portal — the only occurrence of that path is the CLI's own local dashboard route, an unrelated non-networked surface. Flagged provisional in `UsageView`'s doc comment; full trace in `task-2-report.md`. **Task 3 (same plan, same six files -- no new ones):** `GET /api/billing/subscription`, the CLI's plan picker, verified key-by-key against `agent/subscription_view.py`'s `subscription_state_from_payload` (:214), `_parse_current` (:141) and `_parse_tier` (:176). `org`/`context`/`canChangePlan`/`portalUrl` reuse Task 1's exact resolution and money encoding (`billingMoney`, decimal strings). `tiers`/`current` are new: `BillingContractService` gains `BillingPlanSource` (`PaymentConfigService.ListPlans` + `GetGroupInfoMap`, threaded in TWICE -- once per narrow interface it satisfies, alongside the existing `BillingPaymentSource`) and `BillingSubscriptionSource` (`SubscriptionService.ListActiveUserSubscriptions`, a genuinely new wire dependency `ProvideBillingContractService` did not have before -- `cmd/server/wire_gen.go`'s one-line diff is regenerated via `go run -mod=mod github.com/google/wire/cmd/wire`, not hand-edited). `tierId`/`current.tierId` are the owning GROUP id, not a `SubscriptionPlan` id -- Inferno has no subscription-to-plan link, only `(userID, groupID)`, so that is the only stable identity the picker can highlight against. `current` is wire-represented as a present `"current":null` key (never an object of nulls) when the caller has no active subscription -- pinned at the byte level in `server/routes/billing_contract_route_test.go`, not just against the Go struct, because a wrong shape here fails silently on the client (`agent/subscription_view.py:142-149`). `monthlyCredits`/`creditsRemaining` are a documented judgment call: Inferno has no "credits" unit, so `Group.MonthlyLimitUSD` (the plan's own monthly USD cap) and its usage-adjusted remainder stand in, absent (not zero) when the group is uncapped. `pendingDowngradeTierName`/`pendingDowngradeAt`/`cancellationEffectiveAt` stay OMITTED (Inferno has no scheduled-downgrade or cancellation model at all); `cancelAtPeriodEnd` is always `false`, deliberately, because that fact IS always knowable here, unlike the other three. `resolveTiers` reads EVERY plan via `ListPlans`, not `ListPlansForSale`, so a plan the caller is grandfathered into but that is no longer sold still renders (`isEnabled:false`) instead of vanishing from its own current-plan row. Also pins `GET /api/billing/auto-top-up` and `GET /api/billing/subscription/pending-change` at 404 (Step 7) -- the client only ever sends PATCH/PUT/DELETE to those two paths and reads both states out of `/api/billing/state`; full trace, the key-by-key citation table and both mutation diffs are in `task-3-report.md`. **Task 4 (same plan, no new files -- `service/billing_contract.go` + `service/billing_contract_test.go` + `handler/oauth_handler.go` + `handler/oauth_handler_test.go` + `handler/wire.go` + `cmd/server/wire_gen.go`, all already listed above):** the `subscription` object embedded in `GET /api/oauth/account`, snake_case (a DIFFERENT wire contract from Task 3's camelCase `/api/billing/subscription`, verified key-by-key against `hermes_cli/nous_account.py`'s `_subscription_from_payload` (:705-716), reached from `_info_from_account_payload`'s `payload.get("subscription")` (:660)). New `BillingContractService.AccountSubscription` + `BillingAccountSubscriptionView`, reusing Task 3's `resolveCurrentSubscription`/`resolveTiers` rather than a second group/plan mapper; `monthly_credits`/`credits_remaining` parse Task 3's own decimal-string output back to `float64` (`billingMoneyStringToFloat`, lossless round trip through `billingMoney`'s `FormatFloat(-1)` encoding). `tier` is the active group's representative plan's `SortOrder`, found via the `tiers[]` entry with `isCurrent`; omitted, never fabricated, if the active group is absent from the plan catalog. `monthly_charge` is ALWAYS omitted -- `hermes_cli/models.py:685-707`'s `is_nous_free_tier` reads `monthly_charge == 0` as free tier, and Inferno has no honest recurring-charge figure for it; `rollover_credits` has no Go field at all (no rollover concept). `OAuthHandler` gains a `billingSvc *service.BillingContractService` field set via `SetBillingContractService` (mirrors the existing `SetAuthorizeService` setter pattern so the ~3 test files calling `NewOAuthHandler(...)` directly need no changes); `ProvideOAuthHandler` (`handler/wire.go`) gained a `billingSvc` parameter, and `cmd/server/wire_gen.go` moved `billingContractService`'s construction earlier so it can be threaded in (hand-edited: the `wire` codegen binary is not installed in this environment). The whole `subscription` key is omitted, not null, when there is no active subscription or `billingSvc` is unset -- Task 1's precedent, and the client's non-dict branch (`nous_account.py:706-707`) treats both identically. Wire-byte tests assert `tier`/`monthly_credits`/`credits_remaining` decode as JSON numbers (not the decimal strings Task 3's sibling endpoint uses for the same underlying figures) and that `monthly_charge`/`rollover_credits`/`tool_access` never appear on the wire; mutation-proved by temporarily emitting `monthly_charge: 0`, confirming both the service- and handler-level guards go red, then reverting. Full trace and the key-by-key citation table are in `task-4-report.md`. Task 5 adds the 7 `/api/billing/*` WRITE endpoints, all gated on `billing:manage` (a scope `/oauth/authorize` refuses outright, ruling R-5.1 -- so all 7 403 in production today; the scope gate is the deliverable). 2 are IMPLEMENTED over the existing order flow: `POST /charge` (creates a `balance` `PaymentOrder` via `BillingChargeOrderSource`, satisfied by `*PaymentService`'s existing `CreateOrder`/`GetOrder`; `Idempotency-Key` mandatory, missing header is a 400 checked before the coordinator runs, not via its own config-dependent `RequireKey`; reuses `*IdempotencyCoordinator`, this codebase's own pre-existing generic dedup mechanism, not a second implementation) and `GET /charge/{id}` (maps Inferno's 7 order statuses onto the client's `pending`/`settled`/`failed`; an unknown OR foreign id -- NotFound or Forbidden from `GetOrder` -- collapses to `{status:"pending"}`, never 404, never another user's data: a 404 would be a cross-tenant existence oracle). The other 5 are honest refusals with no data dependency: `POST /subscription/preview` -> 200 `{effect:"blocked"}` (the client's own vocabulary); `POST /subscription/upgrade`, `PUT`/`DELETE /subscription/pending-change`, `PATCH /auto-top-up` -> 501 + a real reason, never a fake Stripe status (`requires_action`/`payment_failed`) and never 404 (which would read as "portal unreachable", a worse lie). Files: `service/billing_contract.go` (`BillingChargeOrderSource`, `BillingIdempotencyExecutor`, `Charge`, `ChargeStatus`, `SubscriptionPreview`, `SubscriptionUpgrade`, `PendingChangeSet`, `PendingChangeClear`, `AutoTopUp` + their view types; `NewBillingContractService` gained 2 params), `service/billing_contract_test.go`, `handler/billing_contract_handler.go` (7 new handlers, bare JSON), `handler/billing_contract_handler_test.go`, `server/routes/billing_contract.go` (7 new routes on `service.ScopeBillingManage`), `server/routes/billing_contract_route_test.go` (scope-gate x7, real-second-user cross-tenant proof, wrong-method-405 pinning, idempotency dedup over real HTTP) -- all already declared above (Tasks 1/3/4), no new divergence surface. `service/wire.go` and `cmd/server/wire_gen.go` (also already declared) changed only to thread `*PaymentService`/`*IdempotencyCoordinator` into `ProvideBillingContractService`; `wire_gen.go` was regenerated with the real `wire` CLI (`go run github.com/google/wire/cmd/wire@latest ./cmd/server/`), not hand-edited this time. Full trace, the endpoint table, the status-mapping justification and the mutation-proof output are in `task-5-report.md`. |
| D8 | Agent registry + Chronos cron signer schemas (Task 1: `agents` + `agent_cron_fires` ent schemas and migrations) | `ent/schema/{agent,agent_cron_fire}.go` + `ent/` regeneration (`agent*.go`, `agentcronfire*.go`, plus shared codegen files `client.go`, `ent.go`, `hook/hook.go`, `intercept/intercept.go`, `migrate/schema.go`, `mutation.go`, `predicate/predicate.go`, `runtime/runtime.go`, `tx.go`) + `migrations/911_agents.sql` + `migrations/912_agent_cron_fires.sql` + `migrations/913_agent_cron_fires_dedup_key_per_agent.sql` | Inferno owns agent IDENTITY only (which user/org an agent belongs to, its name, its dashboard URL); VM lifecycle stays in oc-platform. `public_id` is a STRING by design — `apps/desktop/electron/main.ts:7922` silently drops any agent whose `id` is not a string, so it must never become the int64 row id. `agent_cron_fires` is the durable truth behind Chronos's in-memory `TimingWheelService`. `dedup_key` is UNIQUE **PER AGENT** — a composite `(agent_row_id, dedup_key)` index (913, ruling T4-1) — never globally: `dedup_key` is `{job_id}:{fire_at}` and `job_id` lives in the calling agent's own namespace, so two different agents legitimately arm identically-named jobs. 912 originally shipped `dedup_key` as field-level (globally) `.Unique()`, a plan defect from Task 1's brief: it let one agent's `INSERT ... ON CONFLICT` collide into a DIFFERENT agent's row (agent 2 provisioning agent 1's `dedup_key` got HTTP 200 carrying agent 1's `job_id`/`schedule_id`, no row of its own) and it permanently blocked a second agent from ever arming a same-named job. The composite index is the whole idempotency mechanism per agent because the agent's cold-start reconcile re-arms every job it still wants (`plugins/cron_providers/chronos/_nas_client.py:114-120`), so duplicate arms within one agent are normal traffic absorbed at the database rather than by a read-then-write check, while a foreign agent's row can no longer be a conflict target at all. | Additive tables, cannot conflict. Re-run `go generate ./ent` on any upstream schema change and let codegen rebuild from the two schema files. `migrations/913` must apply AFTER `912` in any environment that already ran `912`'s single-column `UNIQUE` — it drops that constraint (named `agent_cron_fires_dedup_key_key`, Postgres's default inline-constraint name) before creating the composite index. |
| D9 | Agent registry service layer (Task 2: `AgentRegistryService` register + list, additive to D8's schemas) | `internal/service/agent_registry.go` + `internal/service/agent_registry_test.go` | `Register` looks the row up by `public_id` FIRST and rejects with `Forbidden("AGENT_PUBLIC_ID_OWNED_BY_ANOTHER_USER")` when it belongs to a different user, THEN upserts — a bare `OnConflictColumns(public_id).UpdateNewValues()` overwrote `user_id`/`org_id` and let any authenticated caller hijack an agent by its client_id, which this credential model treats as PUBLIC. A composite `(user_id, public_id)` conflict target does NOT work: D8 declares `public_id` globally `.Unique()` (correct for a client_id), so there is no composite index to target. `ListForUser` excludes revoked agents and is scoped to BOTH `user_id` AND `org_id`, each independently mutation-proven against a fixture that seeds a same-user-different-org agent — an org-blind list would make Task 3's 409 org picker decorative. `Status` is free-form human-readable prose (`online` / `last seen Xh ago` / `never connected`), never an enum token — `apps/desktop/src/i18n/en.ts:812` renders it verbatim. **IM-1: that prose also fills `DashboardGatewayState`**, which is the field the desktop ACTUALLY renders (`gateway-settings.tsx:1217` passes `dashboardGatewayState`, not `status`, to `cloudStatusLabel`); it shipped as always `""`, and since `""` is a string the desktop's `'unknown'` fallback never fired, so every Cloud-tab row read literally `Status: ` while the derived prose was never displayed at all. The route test asserts VALUES now, not key presence — presence-only is exactly how `""` got through. | Additive, new files, cannot conflict. |
| D10 | Agent registry HTTP surface (Task 3) | `internal/handler/agent_handler.go`, `internal/server/routes/agents.go` + its route test | `GET /api/agents` is the desktop's cloud discovery call and `POST /api/agents/register` is an agent's own boot-time register/heartbeat. Two JSON conventions in one file, deliberately: the GET RESPONSE is camelCase because the DESKTOP parses it (`id`/`name`/`status`/`dashboardUrl`/`dashboardGatewayState`, plus `org:{id,slug,name,isPersonal}`), while the register REQUEST body is snake_case because that endpoint is ours alone. `id` and `org.id` are emitted as JSON STRINGS — `apps/desktop/electron/main.ts:7922` filters `typeof a.id === 'string'` and silently DROPS any agent that fails it. Bare JSON, never the panel envelope, for the same reason. Multi-org callers with no `?org=` get 409 `org_selection_required` carrying the org list the picker reads (main.ts:7849). | Additive, new files, cannot conflict. |
| D11 | Chronos scheduler surface (Task 4): `/api/agent-cron/{provision,cancel,list}` | `internal/service/agent_cron.go` + its test, `internal/server/routes/agent_cron_route_test.go`, plus additions to the already-declared D9/D10 files (`internal/service/agent_registry.go`'s new `ResolveOwnedAgentRowID`, `internal/handler/agent_handler.go`'s three new methods, `internal/server/routes/agents.go`'s three new routes) | An agent arms a SINGLE `fire_at` and re-arms after each fire — Chronos is not a cron engine, there is no recurrence rule. Every request/response key is cited verbatim to `plugins/cron_providers/chronos/_nas_client.py:96-123` (`NasCronClient`), which sends NO agent-id field in any body — the calling agent is identified by its OWN OAuth token's verified `aud` claim (its `public_id`, already established as its client_id by D9), resolved to a row id via `ResolveOwnedAgentRowID`, scoped to the caller exactly like `Register`'s hijack guard. `Provision` is `INSERT ... ON CONFLICT (agent_row_id, dedup_key) DO UPDATE SET updated_at = now(), state = 'armed', attempts = 0, last_error = ''` — never `UpdateNewValues()`, which would silently overwrite `schedule_id` on every idempotent re-arm; a duplicate arm with the same `dedup_key` is normal cold-start-reconcile traffic, not an error, and is mutation-proven to return the same `schedule_id`. **Ruling W-5: an explicit `Provision` on a row that is already `fired`/`cancelled` RE-ARMS it** (state back to `armed`, `attempts`/`last_error` cleared, timer set) rather than answering 200 having armed nothing. The conflict clause originally touched `updated_at` alone, so a terminal row stayed terminal, `FireNow` returned silently for it, `ListArmed` hid it, and the agent's reconcile re-armed into that no-op forever — permanently losing the job. The agent replays an identical `dedup_key` in two real recovery windows (it crashes after a fire but before persisting `next_run_at`; or its own `_list_armed` errors and it re-arms everything, `plugins/cron_providers/chronos/__init__.py:189-215`), so rejecting instead would turn a recoverable crash window into a permanent hard failure. This does NOT weaken the schema's "a `fired` row must never re-fire", which governs REHYDRATION (`RehydrateOnBoot` filters to `state=armed`) — a boot nobody asked for — whereas `Provision` is someone asking; both directions are mutation-pinned (`TestProvisionOnAFiredRowReArmsIt`, `TestRehydrateOnBootRearmsPendingFiresAndSkipsTerminalOnes`). **CR-2: `agent_callback_url` is validated at `Provision`** (`validateAgentCallbackURL`) — https only, absolute, no userinfo, not loopback/unspecified/`localhost` (reusing `oauth_redirect_uri.go`'s `isLoopbackHost`), not a private/link-local/CGNAT/ULA IP literal and not a known cloud-metadata hostname (reusing `channel_monitor_ssrf.go`'s `isPrivateIP`/`isBlockedHostname`), and a DNS name must be dotted (a single-label `https://vault/` only resolves inside a private search domain). It shipped with a `!= ""` check alone, which let a caller aim a server-side POST carrying a portal-signed RS256 JWT at any internal address, at a time of the caller's choosing. This is HALF the fix — see D12 for the dial-time half. `Cancel` and `ListArmed` are both scoped to `agent_row_id` (mutation-proven for `ListArmed`) — `job_id` is not globally unique across agents. Cancelling an unknown `job_id` is a no-op returning 200/nil, never an error — the agent cancels optimistically. Bare JSON, snake_case throughout (this IS a shipping client's hardcoded wire contract, unlike D10's register endpoint), no `agents:read`/`agents:manage` gate. | Additive, new files plus a handful of new methods/routes appended to D9/D10's files, cannot conflict. |
| D12 | Chronos firer (Task 5): mints, POSTs, retries, rehydrates the fires D11 arms | `internal/service/agent_cron_firer.go` + its test, plus additions to the already-declared `internal/service/wire.go` (`ProvideAgentCronFirer`) and `cmd/server/wire_gen.go` (constructed after `timingWheelService`, before `ListenAndServe`) | `mintFireToken` is modeled on `OAuthTokenService.mintAccessToken` and stamps `purpose: "cron_fire"` — cited to `plugins/cron_providers/chronos/verify.py:140` (read-only client repo), which rejects any fire token missing that claim; without it an ordinary agent access token (same `aud`/`exp`/`iss` shape) would be replayable against `/api/cron/fire`. Refuses to mint on a blank issuer (reuses `ErrIssuerNotConfigured`, the same sentinel `mintAccessToken` already uses, since the failure and remedy are identical). `FireNow`'s response contract is cited verbatim to `hermes_cli/web_routers/cron.py`'s own docstring: non-2xx (or a transport error) is retryable — stays `armed`, `attempts++`, `last_error` recorded, re-armed with backoff (`attempts*30s`, capped 10m, well inside the timing wheel's ~1h real capacity). **IM-2: `markRetry` arms the retry BEFORE persisting the bookkeeping**, so a failed `attempts`/`last_error` write can no longer leave the row `armed` with no timer anywhere (go-zero deletes a timer's key once its task has run, so there is nothing to fall back on) — invisible until the next boot, and nothing errors to any caller. `truncateFireError` drops invalid UTF-8 and cuts on a RUNE boundary: `last_error` is built from up to 500 bytes read straight off the agent's socket, and a byte-sliced multi-byte rune is invalid UTF-8, which Postgres rejects — failing the very UPDATE that function exists to keep alive; ANY 2xx is terminal (`fired`, never retried) because a 200 also covers "the agent no longer has this job" — retrying an intentionally absent fire is the bug that contract guards against. **CR-2: fires go out through `newSSRFSafeHTTPClient`** (`channel_monitor_checker.go`), not a bare `&http.Client` — `safeDialContext` re-validates the RESOLVED IP at dial time, which is the half that survives DNS rebinding (a callback host that resolves publicly at provision time can resolve to `169.254.169.254` by the time it is dialled) and the half that covers rows predating D11's validation. Both halves are required and both are mutation-pinned. Firer tests point callbacks at an `httptest.Server` on 127.0.0.1, which production must refuse, so they substitute a plain client on the `httpClient` field; `TestFireNowRefusesALoopbackCallbackURL` exercises the client `NewAgentCronFirer` really builds and proves the refusal is recorded as a retryable failure. `RehydrateOnBoot` re-arms every `state=armed` row into the (in-memory) timing wheel at boot — `TimingWheelService` drops every timer on restart and `agent_cron_fires` is the truth (`ent/schema/agent_cron_fire.go`'s own doc comment); a `fired`/`cancelled` row is never re-armed, and an overdue `fire_at` (server was down) arms with zero delay rather than being skipped. Deliberately does NOT hold a `*AgentCronService` field — that service's surface is scoped to one calling agent (ruling T4-1/T4-2) and `RehydrateOnBoot` needs every agent's armed rows in one boot-time query, so this talks to `dbent.AgentCronFire` directly, the same "no repository layer for this table" choice `agent_cron.go` already documents. KNOWN GAP, not fixed by this task: `AgentCronService.Provision` (D11) does not itself arm anything into the timing wheel — a fire provisioned while the server is already up is armed only on the NEXT boot's `RehydrateOnBoot`, not immediately. The task-5 brief's own interface list is exactly `RehydrateOnBoot`/`FireNow`, with no `ArmNow`; flagged for Task 6 (the real-client conformance run) to confirm whether this is acceptable or needs a follow-up task. | Additive, new files plus a documented insertion in the already-declared wiring files, cannot conflict. |

**Better mechanism for D2 when someone has time:** seed the four titles as a
`login_agreement_documents` settings row instead. Admin-editable, and it touches
no Go at all, which retires this ledger entry.

**Upstream is a one-way source.** Fixes and features flow `Wei-Shaw/sub2api` →
Inferno and never back. We do not open PRs upstream, and no ledger entry is a
"temporary carry until upstream takes it" — every row here is permanent until we
delete the feature. The ledger exists so reconciles stay possible, not to shame
divergence into being small.

**Before adding an entry:** prefer frontend-only, then an existing backend field,
and only then a new entry here. Each entry is a real cost at every future
reconcile — pay it deliberately, for product reasons, not by accident.
`avatar_seed` was checked against riding on the existing `avatar_url` field
first — it cannot, because `SetAvatar` runs `normalizeUserAvatarInput`, which
rejects a bare seed.

### Known-good exceptions, do not "fix" these
- `npm run lint:check` reports one pre-existing eslint error in
  `scripts/june-lint.mjs` (`no-misleading-character-class`, the emoji range).
  It reproduces on a clean tree. Leave it or fix it deliberately, but it is not
  a regression you caused.
- `/admin/audit-logs` has one nested scrollbar: `DataTable`'s `.table-wrapper`
  inside `TablePageLayout`, which scrolls internally **by design**.

---

## Verification that is not a gate, but is required

Type-checking proves nothing about whether a screen looks right. For any visual
change:

1. **Render it.** Dev server on `:5173`, log in, navigate to the actual route.
2. **Measure, do not eyeball.** `getBoundingClientRect`, `getComputedStyle`,
   `scrollHeight` vs `clientHeight`. Numbers in the commit message.
3. **Both themes.** `localStorage.setItem('theme','dark')` then reload.
4. **Two widths minimum**, one of them below 1024 where the rail goes off-canvas.
5. **Seed first if the screen is empty.** A component cannot be verified against
   no data — see "Seeds" below.

### The shell audit — rerun after any layout change
Walks every in-shell route asserting: document must not scroll · card frame must
not drift while scrolled · outer gaps 7/7/7 · no reserved scrollbar gutter · no
nested scrollbar · no new console errors. Last run: **48/49 clean**, the one
being the by-design table-wrapper above. The script is in the session log for
`384cd7d0`; rebuild it from that commit message if needed.

---

## BACKLOG — ordered. Do them in this order.

### 1. `TablePageLayout` height coupling — SMALL, DO FIRST
`src/components/layout/TablePageLayout.vue` hardcodes
`height: calc(100vh - 46px / 62px / 78px)` at three breakpoints. Its own comment
explains why: *"shell__card is a plain block … so this element cannot inherit a
height through it"*. **That stopped being true in `31a75e07`** — the shell is a
flex chain pinned to `100dvh` now.

- Replace the three `calc()` rules with `height: 100%` (or `flex: 1; min-height: 0`).
- **Done when:** the three `calc(100vh` occurrences are gone AND all 15 table
  views still render with `.tpl` height == the card's client height, measured.
- **Risk:** touches every admin table page. Measure `.tpl` on at least
  `/admin/users`, `/admin/accounts`, `/admin/audit-logs` before and after.

### 2. Finish ops — the modal-only surfaces — ✅ COMPLETE — every mounted file under `src/views/admin/ops/` is converted.

| file | legacy utils | commit |
|------|--------------|--------|
| `OpsSettingsDialog.vue` | 93 | `4e716ec0` |
| `OpsErrorDetailModal.vue` | 95 | `2bff989f` |
| `OpsRequestDetailsModal.vue` | 87 | `755828bc` |
| `OpsAlertRulesCard.vue` | 86 | `6fcea767` |
| `OpsErrorLogTable.vue` | 66 | `263ff0b5` |
| `OpsOpenAITokenStatsCard.vue` | 50 | `11d9fc7f` |
| `OpsErrorDetailsModal.vue` | 21 | `11d9fc7f` |
| `OpsDashboard.vue` | 6 | `11d9fc7f` |

`conversion-status.mjs` now reports 129/324 converted files; the two dead ops
cards below have been removed rather than counted as converted. Every mounted
ops surface listed above was opened in a browser against seeded data and
measured, not just typechecked.

**To see the OpenAI token stats card at all:** it is hidden by default. Ops
page → Settings → Advanced settings → "Display OpenAI token request stats".
The flag lives in `getAdvancedSettings`, not in the `settings` table, so there
is no row to flip directly.

#### ✅ Two dead ops cards removed; controls consolidated

`OpsRuntimeSettingsCard.vue` (118 utils, 537L) and
`OpsEmailNotificationCard.vue` (121 utils, 442L) have **zero references
anywhere in `src/`** — no import, no `defineAsyncComponent`, no `<component
:is>`. They are also unmounted in `upstream/main`; they arrived dead in
`d464c0f0` when the upstream frontend was vendored wholesale.

They are superseded by `OpsSettingsDialog.vue`, which calls the same two API
pairs (`get/updateAlertRuntimeSettings`, `get/updateEmailNotificationConfig`)
and renders the same fields.

**Resolution:** the alert-silencing and distributed-lock controls from the dead
runtime card are now editable in `OpsSettingsDialog.vue`, and both dead cards
are deleted. The dialog still sends the complete runtime object to the existing
single PUT endpoint, so fields outside the form continue to round-trip intact.

Silencing is validated before save using the existing RFC3339, positive rule ID,
`P0..P3` severity, and lock-key/TTL rules. Focused coverage lives in
`OpsSettingsDialog.spec.ts`.

### 3. `SettingsView` — the elephant, 12,923 lines / 1,742 utilities
### ⚠️ DEFERRED — needs an owner decision before it can start. Item 4 went first.

**Correction:** this file previously said "seven routes". `INFERNO-BUILD.md`
line 1434 says **nine**. That line is the source of truth.

**Why it is blocked and not merely large.** The same line states: *"part 14
wants a redirect from the old URL and a release note, since admin bookmarks
break."* Splitting `/admin/settings` into nine routes is a user-facing
navigation change that invalidates every bookmark and deep link an operator
has. That is a product decision, not a styling one, and it is not something to
land silently overnight.

Owner picks one:
1. **Nine real routes** (`/admin/settings/general`, `/security`, …) with a
   redirect from `/admin/settings` to the first tab, plus a release note.
   Matches archetype C. Bookmarks to the bare URL survive via the redirect;
   nothing else does, because nothing else exists today.
2. **Nine components, one route**, selected by a tab or a `?tab=` query. Zero
   URL breakage, the 12,923-line file still becomes nine reviewable files, and
   the review problem is solved. Loses the clean per-section URL.
3. **Restyle in place** without splitting. Cheapest, but a 12,923-line SFC
   cannot be meaningfully reviewed, which is the reason the split was proposed.

Recommendation: **2**. It captures the entire reviewability win, which is the
actual blocker, at zero cost to anyone's bookmarks, and 1 stays available later
as a pure routing change once the components already exist.

Once unblocked, either way:
- Split **before** restyling. One section per commit, all six gates each.
- It is on the june-lint `TOUCHED_NOT_CONVERTED` waiver — **remove that entry**
  when the last section lands, and confirm lint still passes with it gone.

### 4. Remaining Chart.js — ✅ COMPLETE

Commit `6db5fef1` removes the final runtime Chart.js consumers:

- `DailyRevenueChart.vue` uses independent Dither strips per currency and for
  order count; it was completed in `b594a5dd` with payment seed data.
- `MonitorTrendChart.vue` uses three Dither strips while preserving zoom,
  localization, gap handling, and the existing metric semantics.
- The live admin `AccountStatsModal.vue` usage trend uses Dither strips. The
  duplicate unmounted `components/account/AccountStatsModal.vue` was reconciled
  and removed in `b594a5dd` before conversion.
- `chart.js` and `vue-chartjs` are removed from `package.json` and the lockfile.

The remaining textual references are migration comments/tests only; no runtime
imports remain. The account modal stays on the `TOUCHED_NOT_CONVERTED` waiver
because its other legacy sections still need the later account-modal pass.

### 5. The rest, by weight
`node scripts/conversion-status.mjs --top 30`. Largest first, except that the
three account modals (`CreateAccountModal` 965, `EditAccountModal` 620,
`BulkEditAccountModal` 293) are one job, not three — they share structure and
are all on the waiver list.

---

## Seeds — a screen with no data cannot be verified

All seeds are `now()`-relative, so re-running re-anchors them. The DB clock
drifts ahead of the seed; if a page reads Idle or "No data", reseed before
concluding anything is broken.

```bash
cd inferno-frontend/scripts/seed
for f in seed-dashboard seed-year seed-models seed-ops seed-ops-dense seed-alert-events seed-users; do
  docker cp $f.sql sub2api-postgres:/tmp/
  docker exec sub2api-postgres psql -U sub2api -d sub2api -q -f /tmp/$f.sql
done
```

See `inferno-frontend/scripts/seed/README.md` for what each one covers.

Seeded so far: a year of dashboard daily/hourly, per-model usage, dense ops
traffic + error logs + hourly metrics, 24 alert events across the full P0–P3
ramp and all three statuses, and 11 users with 30 days of usage.

Local dev login: `admin@sub2api.local` / `36232e0cd5be929e4004e4ca025b100e`.

---

## Rules that are not negotiable

1. **Gate 5.** The backend is read-only. If `upstream/main..HEAD` shows anything
   under `backend/`, `frontend/`, `deploy/` or `docs/`, revert it.
2. **Never weaken a test to make it green.** If a spec fails, decide whether the
   test or the code is wrong and fix that one. `INFERNO-BUILD.md` has a worked
   example of this going right.
3. **Never delete a row, column or control to make a layout tidy.** Reducing
   scope is the owner's call. If something must go, say so explicitly.
4. **Measure before claiming.** "Looks fine" is not evidence. Every completion
   claim in the log so far carries numbers; keep that.
5. **A BEM block may not be named after a bare Tailwind utility** while Tailwind
   is still in the build. `june-lint` enforces this now — it was found the hard
   way when `.ring` inherited Tailwind's blue focus ring for three commits.

---

## Status log — append one line per landed commit

| date | commit | what | files converted |
|------|--------|------|-----------------|
| 2026-08-13 | `0ae68c16` | ops skeleton matched to the real page | 119 |
| 2026-08-13 | `31a75e07` | shell: card scrolls, not the document | 119 |
| 2026-08-13 | `384cd7d0` | card scrollbar hidden, padding squared | 119 |
| 2026-08-13 | `35aab418` | dashboard user-trend chart off Chart.js | 118 |
| 2026-08-13 | `caeb742f` | TablePageLayout derives its height (item 1) | 118 |
| 2026-08-13 | `4e716ec0` | ops settings dialog + NumberField primitive | 120 |
| 2026-08-13 | `2bff989f` | ops error detail modal; 12 cards to a `<dl>` | 121 |
| 2026-08-13 | `755828bc` | ops request details; colour off the default state | 122 |
| 2026-08-13 | `6fcea767` | ops alert rules; list stops showing enum names | 123 |
| 2026-08-13 | `263ff0b5` | ops error log table; 6 hues for a category to 0 | 124 |
| 2026-08-13 | `11d9fc7f` | last 3 ops surfaces — **item 2 complete** | 127 |
| 2026-08-13 | `b594a5dd` | payment revenue chart off Chart.js; duplicate account modal reconciled | 127 |
| 2026-08-13 | `6db5fef1` | remaining Chart.js consumers removed; item 4 complete | 129 |
| 2026-08-15 | `bdba323b` | landed PR #3's upstream reconcile by cherry-pick; sync PR queue emptied | 208 |
| 2026-08-15 | `8bc560dc` | restored the site logo + its sanitiser, dropped by the sidebar rewrite | 208 |
| 2026-08-15 | `3e6888ab` | confirm-dialog helper retargeted at the Button primitive | 208 |

### Open, needs an owner decision — read before the next sync run

1. **Gate 5 is breached locally.** `84a3c4ac` put 19 files under `backend/`: an
   `avatar_seed` user field with full ent regeneration, migration
   `161_add_user_avatar_seed.sql` (a duplicate of the existing 161), and CN→EN
   default legal-doc titles in `setting_public.go`. Not pushed, which is the
   only reason the reconcile routine has not tripped. Rule 1 says revert; the
   uncommitted profile-avatar work depends on the field, so the two decisions
   are one decision.
2. **`inferno-redesign` on the remote is 61 commits behind local.** Every
   reconcile the routine has produced was computed against that stale base,
   which is why all three PRs read as massively conflicting. Pushing fixes the
   routine's view and trips Gate 5 in the same move — see 1.
3. **Local is 124 commits behind `upstream/main`.** Gate 5 only means what it
   claims *after* a rebase; against a stale ref it under-reports (19 files),
   against a fresh one it over-reports (246, of which 227 are just upstream
   moving on). Run it post-rebase or not at all.
4. **june-lint: 845 violations across 273 files**, none from the commits above.
   The denominator grew when the restore commits pulled ~140 half-migrated
   files into scope; a file counts as converted the moment it has a scoped
   `<style>` block. `dead-teal-palette` 476 and `ground-rule-3-two-weights` 271
   are the bulk, concentrated in `UsageTable.vue` / `UsageFilters.vue`.
5. **`AppSidebar.vue:114`** binds `user.avatar_url` to `:src` through a bare
   `.trim()` with no sanitiser — same hole as the site logo, different field.
6. Two gaps PR #3 flagged and left open: `backup.ts` multi-part downloads will
   404 until `BackupView.vue` converts, and `accountUsageRefresh.ts` is ported
   but unwired, so Grok quota cells lack the refresh OpenAI/Codex cells have.
